# 环境就绪状态(2026-06-29 验证通过)

## 已装并验证 ✅
**Windows 侧(用户级,默认位置)**:Python 3.12.10、uv 0.11.25、Node 24.18、gh 2.95、pandoc 3.10;VS Code + Remote-WSL/Docker/DevContainers/Python 扩展;原有 Git 2.54、VS Code 1.126。

**WSL2**:Ubuntu 22.04.5 LTS(用户 `ai4s`),systemd 开启,WSL 盘 ~1TB。
- Docker 29.6.1 + Compose v5.2.0;`docker run hello-world` ✅
- **dockerd 代理**:`/etc/systemd/system/docker.service.d/http-proxy.conf` → `HTTP(S)_PROXY=http://127.0.0.1:7897`(用户 clash 端口),否则拉不动 Docker Hub
- WSL 网络:镜像模式(`C:\Users\25006\.wslconfig`)
- 基础工具:build-essential、cmake 3.22、g++ 11、python3-dev/pip/venv
- **Go 1.24.0** 装在 `/usr/local/go`(apt 的 1.18 太老,项目 go.mod 要 1.24);`ai4s` 的 `~/.bashrc` 已前置 `/usr/local/go/bin`,`GOPROXY=https://goproxy.cn,direct`
- git 全局 http(s).proxy = 7897

## 仓库 ✅
`~/ws/world-model` 已 clone。**注意:子模块未拉**(`third_party/FAST_LIO` 等)→ 用前需 `git submodule update --init --recursive`。

## 项目真实运行方式(读源码确认)
- 工具链管理:**devbox**(go@1.24 / uv@0.11.8 / ruff / lefthook),也可手动(已手动装 go1.24)
- 命令运行器:**just**(`justfile`);核心 `sim_cmd = cd orchestration/sim && go run ./cmd/navlab-sim`
- compose:**顶层 `compose/docker-compose.yaml`**(非 docker/compose)
- 验证通过:`go run ./cmd/navlab-sim list-tasks` → 5 个任务;`doctor` → 全 OK(backend=docker)

## 5 个 sim 任务
| 任务 | 说明 |
|---|---|
| **exploration** | Official-maze exploration gate over Gazebo/SITL(**← GBPlanner 要替换 frontier_lite 的目标**) |
| hover | SITL hover gate + SLAM + landing |
| hover-slam-only | 仅 SLAM 悬停预检 |
| navigation | **Nav2** indoor navigation gate |
| scan-robustness | 扰动 + 扫描鲁棒性 |

## 我(Claude)驱动 WSL 的方式备忘
- 在 Windows 上用 `wsl -d Ubuntu-22.04 -u root|ai4s -- bash <脚本路径>` 跑
- 内联 bash 里的 `$VAR` 会被 PowerShell 误解析 → 一律写**脚本文件**再跑
- **别对 /mnt/c 上的脚本跑 sed**(沙箱会拦 `rm`,也可能损坏文件);脚本用 Write 工具写(LF)

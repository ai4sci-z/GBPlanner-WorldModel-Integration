# 工作铁律(所有执行者必读,违反即返工)

> 本文是执行纪律,不是状态文档。状态见 [CURRENT_STATUS.md](CURRENT_STATUS.md)。
> 详细审查约束见 `~/桌面/ClaudeCode_Reviews/` 的 R003 系列(接管第一读物)。

## 一、实证纪律

1. **实证,不论证**:退出码 0 ≠ 成功。必须自检真实产物(镜像用 `docker images`、测试看输出、
   探针看 json、行为看 BIN/tlog)。"代码级论证兼容"被实测打过脸,不再信。
2. **科学方法**:单变量施工;实验先冻结基线(commit/镜像 digest/配置/场景/命令);
   结果必须可重复;先写会失败的反例证明旧实现真的错,再动手修。
3. **状态词分级**:候选 ≠ 已验证 ≠ 稳定。根因成立四条件:解释现象 + 单变量复现失败 +
   修复后重复成功 + 排除竞争假设。达不到只能写"候选/OBSERVED/UNKNOWN"。
4. **分母纪律**:attempts / airborne / full-pass 三并列,失败永不出分母;
   不用第二次成功关闭第一次失败;诊断臂不冒充默认主线。

## 二、颗粒度纪律

5. **长任务队列里的每一项 = 单独任务的投入**。十个任务不许压缩成一个任务的时间。
   慢才是真的快,不返工才是真的完成。
6. **进真实现场逐行干**:改文档 = 逐行读逐处改;查代码 = 真打开文件 / `git show` 重放核验。
   不许用"需要 checkout"之类假边界绕过只读可验证的事。
7. **不畏难、不迂回、不拖延**:硬骨头当轮死磕;不把"转换困难"包装成进展;不把卡口拖给下一轮。

## 三、文档纪律

8. **正文即正确**:文档改对正文本身,不贴"我改了什么"的过程横幅/审计签名。
   审计溯源归 git 历史与 governance 审计记录,不进文档正文。
9. **死文档进 archive**:历史使命完成的文档 `git mv` 入 `docs/archive/`(单行 ARCHIVE 标记),
   同步全部入链;不留在原地装饰。
10. **唯一事实源**:全局状态只在 CURRENT_STATUS;任务队列只在 TASKS;问题只在 Bug 台账;
    其他文档不复制状态(避免双事实源)。
11. **历史不改写**:原始数据/失败记录不动;被推翻的结论就地标 SUPERSEDED + 指向替代,
    或整档归档。

## 四、收口纪律

12. **六项收口**(每个最小工作单元,成败同待遇):代码或证据 → 验证 → 全部受影响文档 →
    边界清晰 commit → 推授权远端(ls-remote 核对)→ 终端汇报。六项不齐不称完成。
13. **汇报分级**:严格区分"已编码 / 单测通过 / 真实跑过 / 重复验收通过 / 仍阻塞";
    "测试全绿"不替代内容正确;不确定与风险摆在前面。
14. **停点纪律**:到停点停,输出结构化停点报告;沉默/自动测试绿/他人未反对都不是放行;
    唯一放行人 = 项目负责人。

## 五、红线

15. world-model / gbp-feat 治理期只读;world-model 上游(SZ-surveying)永不 push/PR,只推授权镜像。
16. 未放行不启动仿真/容器/负载工具;不打 tag、不改分支/remote、不做破坏性清理。
17. Docker 操作显式指定 context(本机存在 system 与 Desktop 双 daemon,舰队在 system/default)。
18. 取退出码不走管道(先落盘输出,再单独取 `$?`)。

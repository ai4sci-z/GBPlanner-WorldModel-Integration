# 任务台账(进程调度式 · 防丢 + 自纠错机制)

> 项目"调度状态盘",仿操作系统进程表(PCB)。git 提交后永不丢。

## 一、机制

### 1.1 调度 / 防丢(仿 OS 进程表)
- 本文件 = 进程表;状态:⬜就绪 / 🔵运行中 / ⏸阻塞 / ✅完成。
- **检查点**:每完成一最小步 → 写盘 + git 提交 → 任何时刻被打断,已完成部分都在。
- **抢占**:你插入紧急任务 = 高优先级抢占;我先把当前状态写进本表,处理你的事,再回表续跑。
- **后台进程**:长任务(镜像构建、子模块拉取)放后台,完成通知,不阻塞前台。

### 1.2 自纠错(防止"我自己出错还发现不了")
- **文档(md)**:每次更新后自检 →① 不含 base64 等大块乱码;② 引用图片文件都存在;③ 桌面 md 用**绝对路径**引图、README 用相对路径;④ 文件大小正常。不过则修正再提交。
  - ⚠️ 教训:**Typora 不渲染 base64 内嵌图** → 桌面 md 一律绝对路径引 PNG,**禁用 base64**。
- **代码**:每写一段必须在 WSL **编译 + 跑测试**,全绿才算完成、才提交;不绿不标完成。
- **每步收尾(四处同步)**:① 更新桌面 md(文字+图)② 跑上述自检 ③ git 提交推送(权威源 / 桌面传送门[自动] / 桌面 md / GitHub 四处一致)。

## 二、命名约定(消除歧义)
- **预研 A / 预研 B** = 两个**复现任务**(A=复现 world-model,B=复现 GBPlanner)。
- **集成方案**用文字名:**「桥接方案(ros1_bridge)」(已选定)** / **「重写方案(gbplanner_core)」(备选)**。**不再用字母 A/B 指代方案。**

## 三、当前任务表(2026-06-29)
| ID | 任务 | 状态 | 备注 |
|---|---|---|---|
| 1 | 预研 B:GBPlanner 参考镜像 | ✅ 完成 | `gbplanner-ref` 镜像**构建成功**(修复重复包后);跑 rmf_sim 看动画为可选下一步 |
| 2 | 深挖 GBPlanner 接 ROS2 的做法 | ✅ 完成 | 确认 ros1_bridge 官方做法 → 你已选「桥接方案」 |
| 3 | 预研 A:复现 world-model | 🔵 运行中 | 子模块已拉完;下一步 `navlab-sim build` 构镜像(重型,**动手前会确认**) |
| 4 | 桥接路线落地(原 P1 重定向) | ⬜ 就绪 | 见第四节 |

## 四、决策 & 桥接路线(你已拍板)
**集成采用「桥接方案(ros1_bridge)」。** 据此原 P1 重新规划为:
1. 跑通 GBPlanner 的 ROS1 容器(预研 B 的 `gbplanner-ref`),`roslaunch rmf_sim`,确认算法真实 I/O。
2. 搭 **ros1_bridge**:把 world-model(ROS2)的 3D 点云 + 里程计 → 喂给 GBPlanner(ROS1)期望话题;把 GBPlanner/PCI 输出的路径/航点 → 回流到 ROS2 的 `/navlab/exploration/*`。
3. **给仿真无人机 iq_quad 加 3D 雷达**(GBPlanner 需 3D 点云;现仅 2D)。
4. 接 world-model 的 exploration 任务,替换 frontier_lite,过验收闸门。

> 已写的 `gbplanner_core`(光线投射+体积增益,测试通过)→ 转为**加深理解 + 备选实现**,不浪费。
> 📌 建议向 mentor 确认一句"接受 ros1_bridge 方案吗"。

## 五、自检记录
- 2026-06-29 桌面 md:base64 乱码 → 改绝对路径,自检+复检通过(无 base64,20KB)。
- 2026-06-29 P1 代码:cmake 编译 + ctest **1/1 通过**。
- 2026-06-29 预研 B:docker build **BUILD_OK**,镜像 `gbplanner-ref`(10.7GB)生成。

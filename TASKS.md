# 任务台账(进程调度式·防丢机制)

> 这是项目的"调度状态盘",仿操作系统进程表(PCB)。**git 提交后永不丢。**

## 一、机制(最简版,仿 OS 进程调度)
- **本文件 = 进程表**:每个任务一行,带状态。是磁盘上的权威调度状态。
- **状态**:⬜就绪 / 🔵运行中 / ⏸阻塞(等构建或等你决策) / ✅完成
- **规则1 · 检查点**:我每完成一个最小步,就【写盘 + git 提交】→ 任何时刻被打断,已完成部分都在,不丢。
- **规则2 · 抢占**:你插入紧急任务 = 高优先级抢占。我先把当前任务状态写进本表,处理你的事,再回表接着跑。
- **规则3 · 恢复**:被打断/新会话后,先读本表 + 最新 git,找"运行中/阻塞"的任务接着干。
- **规则4 · 后台进程**:长耗时(镜像构建、子模块拉取)放后台跑,完成后回收结果——不阻塞前台。

## 二、当前任务表(2026-06-29)
| ID | 任务 | 状态 | 备注 |
|---|---|---|---|
| 1 | 修复+构建 GBPlanner 参考镜像(预研 B) | 🔵 运行中(后台) | 已修"重复包"bug(改用官方 rosinstall 在工作区根 init);docker build 进行中 |
| 2 | 深挖 GBPlanner 接 ROS2 的官方做法 | ✅ 完成 | 确认 GBPlanner=ROS1,作者用 **ros1_bridge** 接 ROS2(Unified Autonomy Stack)→ 见第三节 A/B |
| 3 | 复现 world-model 基线(预研 A) | 🔵 运行中(后台) | 正在拉子模块(ardupilot 等);之后 navlab-sim build 构镜像 → 跑 exploration |
| 4 | 继续 P1:gbplanner_core 后续模块 | ⏸ 阻塞 | **等你定 A/B 策略**:选 A 则 P1 降为"理解/备选";选 B 则继续深做 |

## 三、待你决策:集成策略 A vs B(task 2 产出)
| | 策略 A:ros1_bridge 桥接(**推荐为先**) | 策略 B:抽 gbplanner_core + ROS2 重写 |
|---|---|---|
| 做法 | 官方 GBPlanner+PCI+voxblox 跑 ROS1 容器,用 ros1_bridge 接 world-model 的 ROS2 | 把算法抽成纯 C++ 核心 + ROS2 外壳 + 换 octomap/nvblox 建图 |
| 优点 | **作者官方验证、最快**;算法原版不改;复用 voxblox(顺带解决"错配2") | 单一 ROS2 栈,干净长远,完全可控 |
| 缺点 | 两套 ROS + 一座桥要维护;ROS1 Noetic 已 EOL(容器里仍可用) | **工作量大**;要重写 RRG/增益/frontier + 建图;行为需对齐原版 |
| 对 P1 影响 | P1 自写核心降为"加深理解/备选" | P1 是主线,继续深做 |
| 预研 B 的作用 | **直接成为策略 A 的运行件**(就是这个 ROS1 容器) | 作参考/对照 |

**我的建议**:这类"把算法落进项目"的任务,通常**先用策略 A 把系统跑通**(作者都这么做),再视需要考虑 B。但这关系到 mentor 对架构的偏好,**建议你和 mentor 确认一句"接受 ros1_bridge 方案吗"**,再定。

> P1 已写的核心(光线投射+体积增益,测试通过)无论选哪条都不浪费:选 B 是主线,选 A 是加深理解+备选实现。

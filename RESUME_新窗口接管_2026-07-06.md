> **[HISTORICAL] 本文已被 [CURRENT_STATUS.md](CURRENT_STATUS.md) 取代**(2026-07-06 深夜文档重整)。
> 新窗口接管:**只读 CURRENT_STATUS.md + TASKS.md + 接力棒_当前值班.md** 即可无损接管。
> 本文仅保留下面这段**工具使用铁律**(仍然有效,防工具调用泄漏 bug):

## 🔴 工具铁律(照做就不漏)

历史 bug:`wsl -d Ubuntu-22.04 -- bash -lc '...'` 里塞嵌套引号/管道/中文/`python3 -c` 时,工具调用会偶发以字面文本泄漏、不执行。原生工具(Grep/Read/Edit/Write)和 PowerShell 全程零泄漏。

1. 搜文件用 **Grep**,读用 **Read**,改用 **Edit/Write**,git 用 **PowerShell**——都不经 shell 引号;
2. 必须在 WSL 跑的(docker/go/python 解析):**先 Write 成脚本文件**(`runbooks/world-model-jazzy/xxx.py|sh`),再用极简命令 `wsl -d Ubuntu-22.04 -- bash -lc 'python3 /mnt/c/.../xxx.py 参数'` 跑;
3. 绝不内联 `python3 -c`、带中文的 grep pattern、多层嵌套引号;`export PATH=...:$PATH` 也会炸(写进脚本文件);
4. `docker exec` 不经过 entrypoint,容器内跑 ROS 命令须显式 `source /opt/ros/<distro>/setup.bash`;
5. 验收 ROS2 话题一律用 **rclpy 订阅**(jazzy `ros2 topic echo` CLI 对 rclpy 发布者收不到,stage2i 实测)。

其余全部状态、阶段、证据、纪律 → [CURRENT_STATUS.md](CURRENT_STATUS.md)。

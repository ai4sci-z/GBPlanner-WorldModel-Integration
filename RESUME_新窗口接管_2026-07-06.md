# 🔄 新窗口接管文档（2026-07-06 · 起飞突破后）

> 新窗口读这份 + MEMORY.md + docs/world-model端到端Bug台账_给作者PR.md 即可无损接管。
>
> 🔴🔴 **必看·工具调用泄漏 bug 的准确诊断与修法（上个窗口栽了 6 次）**：
> 症状=工具调用偶发以字面文本 `court` + `<invoke name="Bash">...` 原文漏出、不执行。
> **准确根源(实测)：只发生在 `wsl -d Ubuntu-22.04 -- bash -lc '...'` 这种 Bash 调用上,尤其命令里有嵌套引号/管道/中文/python -c 转义时。原生工具(Grep/Read/Edit/Write)和 PowerShell 全程零泄漏。**
> **可靠修法(照做就不漏)**：
> ① 搜文件用 **Grep 工具**(原生),读文件用 **Read**,改文件用 **Edit/Write**,git 用 **PowerShell**——都不经 shell 引号,不漏。
> ② 必须在 WSL 跑的(docker/go/python 解析 BIN)：**把逻辑写成脚本文件(Write 到 /mnt/c/.../runbooks/world-model-jazzy/xxx.py 或 .sh)，再用极简 Bash 命令 `wsl -d Ubuntu-22.04 -- bash -lc 'python3 /mnt/c/.../xxx.py 参数'` 跑**——脚本里随便写复杂逻辑,命令行本身无嵌套引号/无中文。
> ③ 绝不在 Bash 命令行里内联 `python3 -c "..."`、带中文的 grep pattern、多层嵌套引号。
> (旧诊断"上下文太长"是错的,已更正——是命令模式问题,不是长度问题。)

## ⭐ 当前状态：🏁 端到端全绿（2026-07-06 晚,无 hack,实测）

### 已达成（实测，非假设）
- 🏁 **exploration 首次端到端全绿**：run `20260706T130626`,`status=TASK_STATUS_OK`、`ok=True`、blockers 空、**4 探针全 ok**(frame_contract 8/8 话题含 /tf_static、/ap/v1/pose/filtered)、`accepted_goals=3/3`、`path_length=1.06m`、`takeoff.ok=True`、landing ok=True。BIN 物理铁证:SIM 地面真值 +0.720m、电机 PWM 峰值 1950。**clean_repro.sh 首次 rc=0**。
- ✅ **clean 分支提交链**：`09a5aa4`(上游) → `79643b9`(5类真bug+B15死锁) → `77d951a`(撤3参数hack) → `dada2db`(B16 探针修复:QoS内省+慢发现预算+测试断言遗留)。净 diff **286 行、零 hack**:`integration/world-model-PR/CLEAN_REPRO_takeoff_fixes.diff`。`go build/vet/test ./...` 全绿。
- ✅ **B16 双根因都实测锤死**：/tf_static=latched QoS(探针改 publisher 内省订阅);/ap/v1/pose/filtered=**DDS 慢发现**(受控实验:后加入订阅对 micro-ROS agent endpoints 匹配需 28.97s,匹配后 40ms 即达;非QoS非时序)→ 等待预算 45s+容器 90s。
- ⚠️ 诚实边界：`slam.ready=False` 仍在(gate 靠 /slam/odom evidence 兜底,不挡全绿);exploration 指标有 run 间波动(accepted_goals 2~3、path 1.06~1.61m),本次 3/3 达标。

### 关键修复清单（都是作者真 bug，PR 料，clean 基线验证过）
1. `%%`→`%`（exploration_workflow_runtime.py.tmpl，text/template 不 Sprintf → py_compile 崩）
2. 空 launch 参数跳过（backends.py，ROS2 拒绝空 name:=）
3. IMU 自吞回声（slam.go+defaults.go：IMU 输出话题从 /imu 改 /navlab/slam/imu，否则 cartographer Non-sorted data abort）
4. 测距仪参数名漂移（navlab_models.go+2 parm：RNGFND1_MIN_CM→RNGFND1_MIN，4.5前旧名被固件忽略→MIN默认0.20m拒0.095m读数）
5. **⭐死锁逻辑修复（关键那一刀，fcu_controller_runtime.py.tmpl）**：`on_setpoint_intent` 加 `if bootstrap_ready(state):` 门——**起飞完成前不把探索 intent 转发给飞控**。否则探索工作流的"保持当前位置"指令(cmd_vel/位置设定点)覆盖 GUIDED takeoff 爬升(CTUN.DAlt 被摁在当前高度)→永不离地→takeoff never ok→控制器 never ready→工作流一直发 hold→死锁。这是相序互斥正解，不是强改 DAlt。
   - ⚠️ 注意：我曾额外堆过 3 个参数 hack(EK3_SRC1_POSZ改气压计/DISARM_DELAY=0/readiness拉长)，被用户正确批评不符合物理实际。去掉后照样飞（SIM+0.76m），证明 hack 不需要。测距仪高度源(POSZ=2)、安全保护都保持作者原样。**撤销已作为 commit `77d951a` 正式提交**（此前只改工作树未提交、diff 未重导，Codex 已查出，现修复）。

### ✅ B16 探针修复（2026-07-06 晚已修并验证,原"剩余 gate"全清）
1. **`/tf_static`(QoS)**：探针订阅改 publisher QoS 内省匹配(`get_publishers_info_by_topic`)→ 实测 ok=True。
2. **`/ap/v1/pose/filtered`(DDS 慢发现,非QoS非时序)**：受控实验(`sub_latency_probe.py`+`run_sub_experiment.sh`)锤死:后加入订阅对 ArduPilot micro-ROS agent endpoints 匹配需 **28.97s**(graph 早见 type、publisher count 持续 0),匹配后 40ms 首条即达;rosbag 因先于 agent 启动而秒配。修法=type 已发现时等待预算提到 45s(`FrameContractSpec.ProbeTimeoutSec`)+容器超时 30→90 → 实测 ok=True。
3. **`exploration_probe`**：本 run accepted_goals=3/3 达标(存在 run 间波动 2~3,基线定档时记录区间)。
- 诊断脚本：`runbooks/world-model-jazzy/` 下 check_probes.py / summary_verdict.py / verify_codex.py / decode_takeoff_bin.py / check_rosbag_qos.py / pose_first_ts.py / sub_latency_probe.py。

## 环境与复现
- 干净克隆：`~/ws-clean/world-model`（作者原版09a5aa4 + 上面5类修复）；兄弟目录 `~/ws-clean/ardupilot_gz`(8df4dc1)。
- jazzy 镜像 9/9 已建、开箱验真过（docker images | grep navlab.*jazzy）。
- 一键跑：`bash /mnt/c/CCproject/GBPlanner-WorldModel-Integration/runbooks/world-model-jazzy/clean_repro.sh`（NAVLAB_SIM_DISTRO=jazzy，从 ~/ws-clean 跑）。**注意：它跑完整 exploration 预检，只要没全绿就永远 rc=1/failed——不代表起飞没成功，要看 run 里的具体指标**（BIN 的 CTUN/RCOU/SIM、summary blockers）。
- 铁律：遇 failed 必停下修那个具体的，不跳过、不假装成功、不将错就错；只报实测数不预告"这次一定成"；变量在 wsl bash -c 里常被吞→写脚本文件执行。

## 用户下一步要的（起飞通了之后）
1. ~~先把 exploration 完整跑完（修 frame_contract_probe）~~ ✅ **已完成(2026-07-06 晚,run 20260706T130626 全绿)**。接下来先做 frontier_lite 基线定档(多跑几次记录指标波动区间,作对照组)。
2. 用**桥接方案(ros1_bridge)**把真 GBPlanner 接进 world-model（不是 gbplanner_gain 那个2D简化版）；物料 `integration/ros1_bridge/`(只有设计稿)。必须先跑通再接、不能盲接。
3. 三个一键 GUI 演示 + 源码级讲解(每个球/线/箭头对源码，不编造)：①原始GBPlanner(预研B,已能飞)②world-model原版frontier_lite③gbplanner接入后。
4. 改的代码都记录，做完经用户同意再提 PR。

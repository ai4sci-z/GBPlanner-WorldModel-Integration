# 🔄 新窗口接管文档（2026-07-06 · 起飞突破后）

> 上个窗口对话极长导致工具调用偶发"court/<invoke>原文泄漏"故障 → 新窗口全新上下文即消失。
> 新窗口读这份 + MEMORY.md + docs/world-model端到端Bug台账_给作者PR.md 即可无损接管。

## ⭐ 当前状态：world-model 端到端起飞突破，只差最后一个探针

### 已达成（实测，非假设）
- ✅ **无人机在 world-model 里真物理离地**：从全新克隆的作者源码(09a5aa4)干净复现，SIM 物理高度升 0.75m、四电机 PWM 1950、DAlt 爬到 0.61m。
- ✅ **原版 frontier_lite 探索首次端到端跑起来**：exploration_probe ok=True、accepted_goals=3、path_length=1.61m、controller_ready=True、takeoff.ok=True state=ready。
- ✅ 修复已提交：clean 分支 `fix/world-model-e2e-takeoff`（commit 79643b9，在 `~/ws-clean/world-model`）。diff 副本：`integration/world-model-PR/CLEAN_REPRO_takeoff_fixes.diff`（213行，用户可看）。

### 关键修复清单（都是作者真 bug，PR 料，clean 基线验证过）
1. `%%`→`%`（exploration_workflow_runtime.py.tmpl，text/template 不 Sprintf → py_compile 崩）
2. 空 launch 参数跳过（backends.py，ROS2 拒绝空 name:=）
3. IMU 自吞回声（slam.go+defaults.go：IMU 输出话题从 /imu 改 /navlab/slam/imu，否则 cartographer Non-sorted data abort）
4. 测距仪参数名漂移（navlab_models.go+2 parm：RNGFND1_MIN_CM→RNGFND1_MIN，4.5前旧名被固件忽略→MIN默认0.20m拒0.095m读数）
5. **⭐死锁逻辑修复（关键那一刀，fcu_controller_runtime.py.tmpl）**：`on_setpoint_intent` 加 `if bootstrap_ready(state):` 门——**起飞完成前不把探索 intent 转发给飞控**。否则探索工作流的"保持当前位置"指令(cmd_vel/位置设定点)覆盖 GUIDED takeoff 爬升(CTUN.DAlt 被摁在当前高度)→永不离地→takeoff never ok→控制器 never ready→工作流一直发 hold→死锁。这是相序互斥正解，不是强改 DAlt。
   - ⚠️ 注意：我曾额外堆过 3 个参数 hack(EK3_SRC1_POSZ改气压计/DISARM_DELAY=0/readiness拉长)，被用户正确批评不符合物理实际，已全部撤销。去掉后照样飞（0.75m），证明 hack 不需要。测距仪高度源(POSZ=2)、安全保护都保持作者原样。

### ◉ 就差最后一个：frame_contract_probe 探针
最新 run 只剩这一个 blocker（其余全绿）。它采不到 2 个话题：
- `/tf_static`：latched(TRANSIENT_LOCAL QoS)，探针用 `qos_profile_sensor_data`(VOLATILE)收不到 → **改探针订阅 QoS 为 TRANSIENT_LOCAL**。
- `/ap/v1/pose/filtered`：ArduPilot DDS 位姿，实测发布过(614条)，探针 ros2_topic_echo/rclpy 采不到，疑 DDS type-hash/时序 → 待查。
- 探针模板：`orchestration/sim/internal/tasks/helpers/templates/python/ros_probe.py.tmpl` L169 `node.create_subscription(msg_type, topic, on_msg, qos_profile_sensor_data)`。
- 诊断脚本：`runbooks/world-model-jazzy/check_probes.py <run_dir>`。

## 环境与复现
- 干净克隆：`~/ws-clean/world-model`（作者原版09a5aa4 + 上面5类修复）；兄弟目录 `~/ws-clean/ardupilot_gz`(8df4dc1)。
- jazzy 镜像 9/9 已建、开箱验真过（docker images | grep navlab.*jazzy）。
- 一键跑：`bash /mnt/c/CCproject/GBPlanner-WorldModel-Integration/runbooks/world-model-jazzy/clean_repro.sh`（NAVLAB_SIM_DISTRO=jazzy，从 ~/ws-clean 跑）。**注意：它跑完整 exploration 预检，只要没全绿就永远 rc=1/failed——不代表起飞没成功，要看 run 里的具体指标**（BIN 的 CTUN/RCOU/SIM、summary blockers）。
- 铁律：遇 failed 必停下修那个具体的，不跳过、不假装成功、不将错就错；只报实测数不预告"这次一定成"；变量在 wsl bash -c 里常被吞→写脚本文件执行。

## 用户下一步要的（起飞通了之后）
1. 先把 exploration 完整跑完（修 frame_contract_probe）。
2. 用**桥接方案(ros1_bridge)**把真 GBPlanner 接进 world-model（不是 gbplanner_gain 那个2D简化版）；物料 `integration/ros1_bridge/`(只有设计稿)。必须先跑通再接、不能盲接。
3. 三个一键 GUI 演示 + 源码级讲解(每个球/线/箭头对源码，不编造)：①原始GBPlanner(预研B,已能飞)②world-model原版frontier_lite③gbplanner接入后。
4. 改的代码都记录，做完经用户同意再提 PR。

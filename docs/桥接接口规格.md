# 桥接接口规格(GBPlanner ROS1 ↔ world-model ROS2)

> 全部字段来自真实源码,**不臆造**:GBPlanner 来自 `gbplanner/launch/rmf/rmf_sim.launch`(引行号);world-model 来自 `docker/profiles/navlab-exploration-foxglove-lite-topics.txt` 与 `configs/tasks/exploration.yaml`。最后更新 2026-06-29。

## 1. GBPlanner(ROS1)真实 I/O
| 方向 | 话题(remap 后) | 消息类型 | 出处 |
|---|---|---|---|
| 输入·点云 | `/<robot>/velodyne_points` | **sensor_msgs/PointCloud2(3D)** | rmf_sim.launch:64 |
| 输入·里程计 | `/<robot>/ground_truth/odometry_throttled` | nav_msgs/Odometry | rmf_sim.launch:59,63 |
| 输入·TF | `world→navigation`、`base_link→velodyne` | tf | rmf_sim.launch:19,25 |
| 触发·服务 | `gbplanner` / `gbplanner/homing` | planner_msgs srv | rmf_sim.launch:72,73 |
| 输出·轨迹 | `/<robot>/command/trajectory` | **trajectory_msgs/MultiDOFJointTrajectory** | rmf_sim.launch:71(PCI) |

## 2. world-model(ROS2)真实话题
| 用途 | 话题 | 消息类型 | 出处 |
|---|---|---|---|
| 里程计 | `/slam/odom` | nav_msgs/Odometry | foxglove profile |
| 2D 激光 | `/scan` | **sensor_msgs/LaserScan(2D!)** | foxglove profile |
| 地图 | `/map` | nav_msgs/OccupancyGrid(2D) | foxglove profile |
| 探索目标/状态 | `/navlab/exploration/{goal,status,coverage,frontiers,path}` | — | foxglove profile |
| 控制意图 | `/navlab/fcu/setpoint/intent` → `/output` | — | foxglove profile + P8设计 |

## 3. 精确桥接映射(就按这个接)
| GBPlanner(ROS1)需要 | world-model(ROS2)对接 | 处理 |
|---|---|---|
| `/<robot>/velodyne_points`(3D 点云) | ❌ **world-model 只有 2D `/scan`** | **见缺口①** |
| `/<robot>/ground_truth/odometry_throttled` | `/slam/odom` | ros1_bridge 直接桥 + 话题改名 |
| TF `world→navigation` | world-model 的 `map`/`odom` 帧 | 加静态 TF 对齐 |
| 触发 service `gbplanner` | bridge/适配节点周期触发 | 适配 |
| 输出 `command/trajectory`(MultiDOF) | `/navlab/exploration/goal` + `/navlab/fcu/setpoint/intent` | **见缺口②** |

## 4. 两个"现在必须定死、后期改不动"的硬缺口

### 缺口① 3D 点云 vs 2D 激光(最关键)
- GBPlanner 的体积增益**必须吃 3D PointCloud2**;world-model 无人机(`navlab_iq_quad`)只有 **2D 平面雷达**(model.sdf:42 `scan_ideal` 361×**垂直1线**)。
- **把 2D `/scan` 转成 PointCloud2 也只是一个平面,撑不起 3D 探索。**
- **结论(早定死)**:必须给仿真无人机**加 3D 雷达**(Velodyne/Livox 多线 gpu_lidar),直接发 `velodyne_points`。否则桥接做完也跑不出真正 3D 探索 → 后期返工。

### 缺口② 输出格式:MultiDOFJointTrajectory vs MAVLink setpoint
- GBPlanner 经 PCI 发 `command/trajectory`(给 RotorS 控制器);world-model 用 **ArduPilot + `/navlab/fcu/setpoint/intent`**。
- **结论(早定死)**:桥接需写一个**适配节点**,把 GBPlanner 的轨迹/航点转成 world-model 的 `setpoint/intent`(经唯一 FCU controller,P8 设计:探索器只能发 intent,不能直发 cmd_vel)。

## 5. ros1_bridge 注意
- 自定义消息(`planner_msgs`、`MultiDOFJointTrajectory`)需在 bridge 显式映射;标准消息(Odometry/PointCloud2/LaserScan)自动桥。
- 详细桥接清单在桥接落地阶段(任务#4)按本规格逐条实现。

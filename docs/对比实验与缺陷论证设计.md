# 对比实验与缺陷论证设计(任务 #5 + #6)

> 目的:① **用实据论证 frontier_lite 不足**(不空口);② **GBPlanner vs frontier_lite 量化对比,突出优势**。
> 所有参数引真实源码;待实测的数字等 exploration 跑通后填入。最后更新 2026-06-29。

## 1. frontier_lite 的真实样貌(已从源码确证)
| 项 | 值 | 出处 |
|---|---|---|
| 策略 | `frontier_lite`(P8 文档原话:"bounded frontier-lite / corridor probing"最小探索闭环) | exploration.yaml:20 / P8设计 |
| 探索窗口 | **仅 26 秒** | exploration.yaml:21 |
| 速度 | **0.10 m/s**(极慢) | exploration.yaml:22 |
| 通过门槛 | 凑 **≥3 个目标点**、路径 ≥0.35m | exploration.yaml:23-24 |
| 用的传感器/地图 | **2D** `/scan` + Cartographer **2D** `/map` | P8设计 §3 / model.sdf |
| 明确不做 | 不是完整 Nav2;不覆盖全部可达空间 | P8设计 §3 |

→ **缺陷论点(有据)**:frontier_lite 只是个"证明流水线通了"的 **2D 占位探索**,**只在平面上凑几个点**,既不立体、也不追求探索效率/覆盖最大化。

## 2. GBPlanner 的真实样貌(已从源码确证)
| 项 | 值 | 出处 |
|---|---|---|
| 决策 | **3D 体积增益**(光线投射数未知体素)+ RRG 图搜索 | gbplanner.cpp/rrg.cpp |
| 传感器 | **3D** OS064(360°×90°/20m) | gbplanner_config:21-29 |
| 地图 | 3D 体素 0.2m(voxblox TSDF/ESDF) | voxblox_sim_config:5 |
| 速度上限 | **1.0 m/s** | gbplanner_config:121 |
| 增益权重 | unknown_voxel_gain 60;距离罚0.04;转向罚0.1 | gbplanner_config:139-143 |

## 3. 对比维度(报告主表)
| 维度 | frontier_lite | GBPlanner | 量化指标 |
|---|---|---|---|
| 探索空间 | 2D 平面 | 3D 立体 | —— |
| 覆盖增长 | 待实测(world-model 运行时修复中) | **实测:地图峰值 132,091 体素点,全程增长曲线已录**(70 采样点,box 迷宫) | tsdf 点数 / `coverage.known_cell_growth` |
| 路径长度 | 待实测 | **实测:全程 291.3 m / 435 s** | `path_length_m` |
| 用时/效率 | 26s 窗口 | **实测:480s 预算,t=435s 自动返航**(`HOMING ENGAGED`) | 探索面积/用时 |
| 速度 | 0.1 m/s | ≤1.0 m/s(实测巡飞 ~0.5-1m/s) | —— |
| 决策依据 | 2D 边界(实为脚本循环,连地图都不订阅) | 3D 体积增益(实测每轮迭代读图选路) | —— |
| 垂直探索 | 不能 | 能(实测飞行高度 1.2~2.6m 变化) | —— |
| 任务管理 | 无 | **实测:时间预算+自动返航+地图落盘** | —— |

> ⚠️ 口径诚实标注:GBPlanner 列的实测数据来自**预研B 官方仿真环境**(RotorS/box 迷宫,2026-07-02,
> 数据文件 `images/exploration_metrics_*.csv/png`、地图 `explored_map_lightboxes.vxblx`);
> frontier_lite 列需 world-model 运行时修好后取 `summary.json`。**同场景同口径的严格对照**(§4)仍待
> 桥接+3D 雷达完成后进行——当前两列可作"能力级"对比,不可当"同场景"对比使用。

## 4. 对比方法(保证公平)
- **同一 Gazebo 场景、同一起点、同一时长**跑 frontier_lite 与 GBPlanner。
- 都从 `summary.json` / rosbag 取**同口径指标**(coverage、path、area、time)。
- 截图同一时刻的地图覆盖对比。
- GBPlanner 需先满足前置:给无人机加 3D 雷达(见 [桥接接口规格.md](桥接接口规格.md) 缺口①)+ ros1_bridge。

## 5. 预期结论(待实测验证,不预先下定论)
预期 GBPlanner 在**覆盖率、探索面积、垂直空间探索**上显著优于 frontier_lite,代价是计算更重。**具体数字以实测为准**,跑完填表。

## 6. 待办
- [x] ~~world-model 9 镜像~~ ✅ 9/9 已建(2026-06-30)。
- [x] GBPlanner 独立环境实测数据 ✅(2026-07-02,预研B 全闭环+指标采样,见 §3 注)。
- [ ] world-model 运行时跑通(tomllib/空参/gazebo-sensor venv 三坑已修,`/scan` 端到端验证待跑)→ frontier_lite 取 summary.json 真实指标 + 截图。
- [ ] 桥接 + 3D 雷达就绪 → **同场景同口径**严格对照 + 覆盖对比图。

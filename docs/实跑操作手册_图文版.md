# 实跑 / 演示 / 自查操作手册(图文版)

> 用途:**组会汇报当天**照着做——既能现场演示,又能自己动手核验每一条成果(只信真实产物,不信嘴)。
> 每步给「敲什么 + 看到什么 + 证明了什么」。最后更新 2026-06-30。
> 📷 说明:示意图是我画的(标注「示意」);真实仿真 GUI 截图待跑通补,不放假图。

---

## 0. 一图看清:今天能演示/自查什么

| 成果 | 怎么看 / 演示 | 证明了什么 |
|---|---|---|
| ⭐ 算法核心可运行 | §3 跑 `gbplanner_core` 体积增益演示(30 秒) | 真把 mentor md 的"光线投射+体积增益选路"跑起来了 |
| 9/9 镜像构建成功 | §2 `docker images \| grep navlab` 数 9 个 | world-model 平台环境在本机复现成功 |
| 连修 3 个 humble 真 bug | §4 看分支 4 个 commit + 运行时日志证据 | 给作者的真实可提交贡献;把 SLAM 从"一崩"推到"真运行" |
| gbplanner_gain 决策层集成 | §4 看 world-model 分支 + 渲染脚本证据 | 决策内核接进真实结构(读图选向替脚本) |
| 阶段4 桥接地基 | §5 看 `integration/ros1_bridge/` | 真版 GBPlanner I/O 契约(源码证实)+ ROS2 适配器 |
| PPT + 全套文档 | 桌面 `★GBPlanner项目进展汇报.pptx` | 汇报材料 |

> **现场演示首选 §3**(确定能跑、最直观);其余靠命令自查,truthful。

---

## 1. 打开 Linux 终端
开始菜单搜 **Ubuntu**(或 **wsl**)→ 回车,出现 `ai4s@AI4S:~$` 即进入。所有命令在此敲。

## 2. 看镜像齐不齐(9/9)
```bash
docker images | grep navlab        # 数到 9 个 navlab/*:humble
docker images | grep gbplanner     # GBPlanner 参考镜像 gbplanner-ref
```
**看到**:9 个 navlab humble 镜像 + gbplanner-ref。**证明**:两套环境都在本机就绪。

---

## 3. ⭐ 现场演示首选:gbplanner_core 体积增益决策(真能跑,30 秒)
这是整个任务里**最直观、确定能跑**的成果——纯 C++、零基础设施,直接对应 mentor md 的算法核心。
```bash
cd /mnt/c/CCproject/GBPlanner-WorldModel-Integration/code/gbplanner_core
# 按该目录 README / 演示文档操作(编译 + 跑 demo)
```
**看到**:对各方向算体积增益、选增益最高且避障的方向;朝开口(未知最多)增益最高被选中、朝墙增益低。
配合桌面决策图 `images/gain_decision.png` 讲解。详见 `docs/算法核心演示_体积增益选路.md`。
**证明**:"看哪里没探过就往哪里去"的核心逻辑真的实现并跑通了。

---

## 4. 自查:给 world-model 的真实代码贡献(4 个 commit)
集成 + 修复都在 world-model 的本地分支上,可亲自核验:
```bash
cd ~/ws/world-model
git log --oneline -4 feat/gbplanner-gain-exploration-strategy
```
**看到 4 个 commit**:
1. `fix` tomllib → 回退 tomli(SLAM 头号崩溃,Py3.10 无 tomllib)
2. `fix` 空 launch 参数(humble 拒绝 `name:=` 空值,SLAM 起不来)
3. `fix` 模板 `%%`→`%`(生成脚本无法编译)
4. `feat` gbplanner_gain 读图选向策略(替换脚本式 frontier_lite)

**修复的实测证据**(只信真实产物):
```bash
# tomllib 已越过:本次重跑 SLAM 日志里 tomllib 出现 0 次
grep -c tomllib ~/ws/world-model/artifacts/sim/exploration/20260630T075417.598290563Z/runtime/logs/slam_backend.runtime.log
# 空参数已修:malformed 0 次,且 cartographer_node 真正运行
grep -c 'malformed launch argument' ~/ws/world-model/artifacts/sim/exploration/20260630T075417.598290563Z/runtime/logs/slam_backend.runtime.log
```
**证明**:连修两个真 bug,把 SLAM 从「一启动就崩」推进到「cartographer 节点真正运行、只差激光数据」。
渲染脚本能编译的证据见 `integration/world-model-PR/rendered_*.py`;完整排错链见 `docs/运行时排错记录_humble.md`。

---

## 5. 自查:阶段4 桥接地基(接真版 GBPlanner)
锁定方案是用 `ros1_bridge` 接 ROS1 原版 GBPlanner。已产出的地基:
```bash
ls /mnt/c/CCproject/GBPlanner-WorldModel-Integration/integration/ros1_bridge/
# bridge_topics.yaml(话题映射) trajectory_to_intent.py(ROS2出口适配器) README_阶段4桥接设计.md
```
**要点**(源码逐条证实):跨桥全是标准消息(点云/里程计/轨迹),自定义 `planner_msgs` 留 ROS1 内不跨桥
→ ros1_bridge 开箱即用(最大的坑被绕开)。详见该目录 README。

---

## 6. 跑 world-model 探索(诚实:运行时调试中)
```bash
cd ~/ws/world-model/orchestration/sim
go run ./cmd/navlab-sim run exploration --live-preflight
```
**当前真实状态**:9 个服务能起;连修 tomllib + 空 launch 参数后,**SLAM 的 cartographer 节点已真正运行**,
但还差 `/scan` 激光数据到达(下一个 humble 坑,待续)→ 尚未出 `summary.json` 的真实覆盖率指标。
> 📷 真实 GUI 截图待 SLAM 完全跑通后补。**今晚汇报如实讲到这一步即可**。

## 7. 路线A:GBPlanner 官方仿真(GUI,镜像就绪,未亲验)
```bash
cp -r /mnt/c/CCproject/GBPlanner-WorldModel-Integration/runbooks/gbplanner_ref ~/gbplanner_ref
cd ~/gbplanner_ref && bash build_and_run.sh
```
**预期**:桌面弹 Gazebo(隧道+无人机)+ RViz(3D点云+探索图+航点)。
> ⚠️ 诚实标注:这条 GUI 弹窗我还没亲自验证,跑通后补真截图。

## 8. 截图 / 排错
- 截图:`Win + Shift + S` 框选 Gazebo/RViz → 粘贴保存;或让我截图存 `images/` 并同步。
- 不弹窗:确认在 WSL2(WSLg),`echo $DISPLAY` 非空。卡/黑屏:软件渲染,等编译完更流畅。

---

## 汇报口径(一句话)
> 这几天:看懂了系统、定下桥接方案、**让算法核心真跑起来**、把决策层接进真实结构、**连修 3 个 humble 真 bug
> 把平台从"装不起来"推进到"SLAM cartographer 真运行"、并备好给作者的 PR/Issue。真正的端到端(3D雷达+起桥)是后续。

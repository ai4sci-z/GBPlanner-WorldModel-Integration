> **[ACTIVE]** 桥接技术主文档(B2.5 自写薄桥)。当前状态以 [CURRENT_STATUS.md](../CURRENT_STATUS.md) 为准。

# B2.5 自写薄桥接真 GBPlanner:查证结论与执行计划(2026-07-06)

> 命名说明:本方案曾名"ros1_bridge 方案";官方 ros1_bridge 与 zenoh 均已实验判死(见选型 B),现行=**B2.5 自写 TCP 薄桥**。

> 前置已达成:world-model frontier_lite **端到端全绿**(run `20260706T130626`,见 Bug 台账 B16)。
> 本文 = Codex《GBPlanner_ros1_bridge_预研建议》查证清单的**实测/源码级回答** + 分阶段执行计划。
> 原则(Codex,采纳):不跨桥 planner_msgs;不用 2D 冒充 3D;每步有 run id/topic hz 证据;先跑通再接。

## 一、查证结论(已完成的静态查证,均有出处)

| # | 查证项 | 结论 | 证据 |
|---|---|---|---|
| 1 | world-model 有无 3D 点云 | ❌ **全 2D**。`navlab_iq_quad/model.sdf` 两个激光 + 世界文件里的传感器全部 `vertical samples=1`;仓内无任何 3D lidar 模型 | grep `<vertical>` 全仓,3 处全是 samples=1 |
| 2 | gbplanner-ref 镜像是否还在 | ✅ `gbplanner-ref:latest`(10.7GB)在本机 docker | `docker images` 实测 |
| 3 | fcu intent JSON 契约 | **速度型**:`linear_x_mps`/`linear_y_mps`/`yaw_rate_radps`(fcu_controller_runtime.py.tmpl L372-405)。→ 适配器必须把 GBPlanner 的位置轨迹转**速度指令**(朝当前 waypoint 的 P 控制),只取 transforms[0] 不够 | 源码 grep |
| 4 | exploration gate 需要什么 | `/navlab/exploration/status`(JSON String)带:`accepted_goals`、`min_accepted_goals`、`path_length_m`、`min_path_length_m`、`ok`、`blockers`、`claim`、`strategy`、`motion_speed_mps`(gate_evaluation.go L224;fcu_controller L346-351 用 `ok` 驱动 task_completed)。**exploration gate 不需要 coverage**(coverage_growth 属 navigation task,L232) | 源码 grep |
| 5 | B15 死锁会否重演 | ✅ **不会**:我们修的 bootstrap_ready 门就在 fcu_controller 消费端,起飞完成前任何 intent(含桥接适配器发的)都不会转发给飞控 | dada2db 已验证 |
| 6 | 探针/DDS 坑对桥接的影响 | 桥容器也是"后加入 participant",对 micro-ROS agent 话题同样要 ~29s 发现(B16 实测);好在桥不订阅 /ap/v1/* 话题,只订 /slam/odom(cyclone↔cyclone,秒配) | B16 受控实验 |

## 二、两个待定选型(需要用户拍板)

### 选型 A:3D 点云怎么来(Codex 风险A,正式对比的硬前提)—— **方案已升级(2026-07-06 晚查证)**
| 方案 | 内容 | 工作量/风险 |
|---|---|---|
| **A1'(现行,官方组件拼装)** | 冒烟仍用 2D scan→PointCloud2(仅验桥路,**绝不当 3D 效果宣称**);正式 3D = **复用 ardupilot_gz 官方 `lidar_3d` 模型**(实测确认:`gpu_lidar`,**360×60 线**,垂直 ±30°,15Hz,topic=`lidar`;`iris_with_lidar` = iris + lidar_3d)——给 `navlab_iq_quad` include/加同参数传感器;**world-model bridge 模板已预留 `/lidar/points`→`cloud_in`(gz.PointCloudPacked→PointCloud2)桥路**(bridge_override.yaml.tmpl L37-41),ROS 侧现成 | 大幅低于自研:SDF 加 include+pose;桥路零改;SLAM(2D /scan)不受影响 |
| A2 | 自研 3D lidar 传感器参数 | 不必——官方组件在 |

### 选型 B:ROS1(Noetic)↔ROS2(jazzy) 桥选型(Codex 清单#4)—— **已实验判定(2026-07-06 晚)**
| 方案 | 内容 | 实验结论 |
|---|---|---|
| ~~B1~~ | 官方 `ros:foxy-ros1-bridge` 镜像 | ❌ **判死(stage2a/2a2 实测)**:①foxy→jazzy 方向通(F2J_RC=0,fastrtps→cyclonedds);②**jazzy→foxy 方向 RMW 层崩**——foxy 的 FastDDS 2.0.x 解析 jazzy 发布者 discovery 数据时 `std::bad_alloc` core dump,**CLI 与 rclpy 双实验同崩**(证据 stage2a_evidence.txt/stage2a2_evidence.txt)→ dynamic_bridge 进程(rclcpp 订阅者)同样会崩,ROS2→ROS1 方向不可用;③foxy 镜像无 rmw_cyclonedds 可换。附:4 个核心消息(Odometry/PointCloud2/MultiDOFJointTrajectory/PoseStamped)**顶层字段 foxy↔jazzy 完全一致**(首轮 DIFF 为 interface show 递归展开的假阳性) |
| ~~B2~~ | zenoh 双桥(`zenoh-bridge-ros1`+`zenoh-bridge-ros2dds`) | ❌ **判死(stage2b/2c/2d 实测)**:rosrust TCPROS `header mismatched` 对 roscpp/rospy 全败(beta.2 与 nightly dev-84 均复现,276 错误),双向 topic 未形成通路 |
| **B2.5(现行)** | **自写 TCP 薄桥**:`thinbridge_ros1_side.py`+`thinbridge_ros2_side.py`,TCP:7601,长度前缀+JSON(cloud3d=base64 直通),心跳保活,frame 桥层映射 | ✅ transport 三段+消费闭环+3D 数据链全 PASS(stage2e/2j/2k/26/3/35) |
| B3 | 源码编 ros1_bridge(humble+Noetic from source) | 备胎,工作量大 |

## 三、分阶段执行计划(采纳 Codex 阶段0-5,含验收)

- **阶段 0 ✅ 已完成**:frontier_lite 全绿(run `20260706T130626`)+ 基线定档(批跑进行中,结果写入本目录)。
- **阶段 1 ✅ 已完成(2026-07-06 晚,实测)**:gbplanner-ref 单侧复验通过——三节点在、voxblox tsdf 4.098Hz、起飞 z=1.2008、automatic_planning success、**自主移动到 (5.73,0.26,1.39)**、`/rmf_obelix/command/trajectory` 抓到实轨迹(证据 `runbooks/world-model-jazzy/stage1_evidence.txt`)。**关键新事实**:①trajectory 是**事件式**发布(每次规划一条,非连续流)→适配器按"收到一条跟踪一条"设计;②waypoint 的 velocities/accelerations 全 0,只有位置+四元数+time_from_start(50ms 步长)→**必须做位置→速度转换**;③**TF 对照(实测)**:GBPlanner odometry=`world→rmf_obelix/base_link`、点云 frame=`rmf_obelix/rmf_obelix/velodyne`(嵌套命名空间)、trajectory frame=`world`;world-model=/slam/odom `map→base_link` → 桥接需 `world↔map` 映射(静态 TF/改 launch 的 world_frame 参数)。
- **阶段 2 ✅ PASS(2026-07-06 晚,B2.5 自写薄桥)**:transport 三段实测通——①ROS2→ROS1 odom(字段原样,34条,stage2e A)②ROS2 scan→ROS1 PointCloud2(width=3,stage2e B)③ROS1 trajectory→ROS2 `/gbp/trajectory`(rclpy 订阅收 2 条 145/180wp,frame 已映射 world→map,stage2j)。**两条实验教训**:a)jazzy `ros2 topic echo` CLI 对 rclpy 发布者收不到(stage2i 矩阵:rclpy→rclpy 60/60、rclpy→CLI 0)——**验收一律用 rclpy 订阅**;b)⚠️ 遗留(不绕过):TCP 在 2~3 条后被对端关闭且重连静默失败(两端异常日志不全)→ **阶段 2.5 必修**:双端 connect/send 失败日志 + ping 心跳 + SO_KEEPALIVE,复跑锁根因后才进阶段 3。
- **阶段 2.5 ✅(2026-07-06 深夜)**:TCP 断连双根因锁定并修复——①空闲被掐→3s ping 心跳+SO_KEEPALIVE(120s+ 稳定 6/6);②acceptor accept 抛 timeout 曾 break 线程死亡→断线永不能重连(stage2k 日志锁定)→改 continue。
- **阶段 2.6 ✅ 消费闭环打通(2026-07-06 深夜,stage26_evidence)**:纯 planner 栈(`wm_planner.launch`,无 gazebo/RotorS,wall clock)+ 薄桥 TF 广播(world→base_link from /wm/odom)。三级验收全过:a)**订阅关系指向过桥输入**(/wm/odom→gbplanner_node+pci;/wm/points→gbplanner_node);b)**voxblox 用 world-model 点云建图**(tsdf 2.303Hz);c)**规划输出真出**(19 条 trajectory,jazzy /gbp/trajectory 收到)。桥全程零断连(odom 614/cloud 296)。**诚实边界:输入仍 2D 冒烟(z=0),3D 与"trajectory 随输入变化"对照留 3D 阶段。**
- **阶段 3 ✅ dry-run PASS(2026-07-06 深夜,stage3_evidence)**:`traj_dryrun.py`(零发布)37 条跟踪量全自洽——frame=map、dist=0.18(勾股核对)、yaw_err=0.88(atan2 核对)、大偏航减速+限幅正确;**且 2D 输入下 waypoint 已见 z 抬升(1.00→1.09,GBPlanner 3D 本性初证)**。语义发现:PCI 规划循环靠"轨迹被执行"驱动,静止 odom 会挂起循环(fresh 触发即出)→ 持续探索需阶段 4 FCU 闭环,dry-run 只能验单批。(注:首跑曾 FAIL——PCI 状态机被上一轮静止 odom 挂起,fresh 重启时序后 PASS;尸检记录在 stage3_evidence 历史。)
- **阶段 3.5 ✅ 3D 数据链贯通(2026-07-07 凌晨,stage3d+stage35_evidence)**:破案链=exploration 实际 spawn 的 overlay include `lidar_2d`(生成器 `navlab_models.go` L35 **故意**把 iris 的 lidar_3d 降为 2D 配 SLAM 链)→ 净增量补丁(`patch_lidar3d.py`):overlay 加独立 `lidar3d`(360×30,±30°,5Hz,topic=/lidar3d,**不动 /lidar 与 x2/SLAM 链**)+ bridge 加 `/lidar3d/points→wm/cloud3d`;go build/test 全绿。**判据①(点云源 3D)**:`/wm/cloud3d` 10800 点,z∈[-0.334,+2.969] 跨度 3.3m(stage3d)。→ 薄桥 cloud3d 直通(base64/2Hz,3D 优先自动停 2D 冒烟)→ **史诗同框(stage35)**:world-model 真栈(SITL 真 odom 604 条+3D 点云 115 帧)喂 GBPlanner:**判据②(voxblox 3D 体素)tsdf 90,472 点 zspan=13.2m ✅**;**判据③(trajectory z)**回流 32wp,z 分量存在(平坦区起步 zspan=0.09m,**只能叫"初步成立"**,大 z 机动待 Stage4/5 或专门对照)。
  **⚠️ status=blocked 定性(勿被"三判据过"掩盖)**:stage35 里 world-model run 自身 `status=blocked`(exploration_probe rc=20)——这是**预期结果**:①frontier_lite 的 accepted_goals 波动是基线已知行为(达标率 40%);②GBPlanner 侧尚无 Stage4 FCU 执行闭环与 Stage5 exploration status/gate 字段,world-model 的 gate 当然不会因 GBPlanner 而通过。**Stage3.5 证明数据链贯通,不证明完整 task/gate 通过。**
- **阶段 4a/4b ✅ 消费直证(2026-07-07 凌晨,stage4a/4b_evidence)**:安全版适配器(fail-closed 默认 disabled、/gbp/enable 显式解锁、/gbp/kill 一票禁用、限速 0.08/yaw 0.30、超龄 30s 弃轨迹、世界系速度投影)INTENT+kill 实测;**`/ap/v1/cmd_vel` 出现 GBP-SIGNATURE**(lin=-0.024/ang.z=-0.300 逐位吻合;唯一性:frontier 值域 yaw 0.18/0.20/-0.12、lin 0.10/0.06 不可能产生)。⚠️ 均在 frontier_lite **混流**下跑。
- **阶段 4c ⬜(当前)**:external 去混流联跑——验运动可归因(验收:无 frontier intent + GBP 签名 + odom path 增长≥0.10m 与 intent 时段对齐 + takeoff ok)。材料已备:`patch_external_strategy.py`(workflow external 策略,**已应用未联跑**)+ exploration.yaml strategy 入口 + 适配器 status(gate_ok_draft 仅报告,ok 保守 False 防误触发 landing——Review_015 风险B)。
- **阶段 5a**:status/gate 严格对齐(ok=True 门槛:external 去混流+path 达标+≥3 wp+blockers 空+起飞完成);5b 3D 行为对照(改场景验 voxblox/trajectory 随输入变化);5c 同口径对比(3~6 run vs 基线 40%)。
- **GUI 三演示**:阶段 5 全部跑通后建(用户指示)。

## 四、PR 政策(用户 2026-07-06 指示)
world-model 源码改动(clean 分支 `fix/world-model-e2e-takeoff`,4 commit,净 diff 286 行)**先保存不提交**;**等最终集成任务(真 GBPlanner 接入)跑通后,再准备完整 PR 物料一并定稿**。

## 五、当前真实架构(B2.5 自写薄桥,Codex 桥接 review 采纳)

```
world-model ROS2(jazzy)                            GBPlanner ROS1(noetic, gbplanner_ref 纯 planner 栈)
  /slam/odom(SITL 真里程计)──┐                          ┌── /wm/odom(+桥广播 world→base_link TF)
  /wm/cloud3d(lidar3d 真3D)──┤ thinbridge_ros2_side.py  │   /wm/points(PointCloud2:3D 直通)
  /scan(2D 冒烟,收到3D后自动停)│ odom 10Hz│cloud3d 2Hz    │        │(wm_planner.launch remap 给 gbplanner/voxblox)
                             └── TCP :7601(4B长度+JSON,ping 3s)── thinbridge_ros1_side.py
                             ┌───────────────────────────┘
  /gbp/trajectory(frame=map)◄── trajectory JSON ◄── /rmf_obelix/command/trajectory(PCI 粉线,事件式)
        │
        ▼(阶段3 dry-run ✅ → 阶段4a/b/c 全过 ✅ → 阶段5a 机制 PASS ✅:gate 以 gbplanner 策略过 exploration_probe)
  trajectory_to_intent_stage4.py(fail-closed 全套 + Procrustes 在线坐标对齐 + 诚实 wp 计数 + 五条件 ok 闩锁)
        │ 位置航点→速度意图(intent 契约=linear_x_mps/linear_y_mps/yaw_rate_radps;fcu 主路实为"胡萝卜"位置目标,见 stage5a_diagnosis)
        ▼
  /navlab/fcu/setpoint/intent(+阶段5 严格 gate 字段 /navlab/exploration/status)
```

**关键分界线(Codex P1 + Review_008,记住)**:阶段 2 只证明 transport;**"GBPlanner 接入 world-model"的判据 = GBPlanner 真的消费 /wm/odom + /wm/points 做规划**(阶段 2.6:remap/relay 到 gbplanner/voxblox 实际订阅的 topic,验证 trajectory 随输入变化),否则只是"两个仿真系统旁路通信"。**验收标准不是"topic 有数据",而是"每一段数据都被下一段真实消费"。**

**Review_008 增补要点(2026-07-06 深夜采纳)**:
1. **粉线/绿线**:桥只接 **`/rmf_obelix/command/trajectory`**(PCI 执行轨迹,粉线);`/vis/best_planning_paths`、`/vis/ref_path` 等 Marker 可视化 topic(绿线)**绝不当控制接口**——薄桥代码本就只订 command/trajectory ✓,阶段2k 用 rostopic info 确认其发布者=PCI。
2. **2D≠GBPlanner 无 3D 能力**:当前 /scan→z=0 点云只是桥路冒烟;正式判据=①PointCloud2 的 z 有真实变化 ②voxblox TSDF 在 3D 体素层更新 ③trajectory waypoint 有合理 z 变化 ④RViz 地图有高度结构。3D 输入=选型 A1'(官方 lidar_3d)。
3. **阶段 2.6 的隐坑(TF)**:GBPlanner 需要 TF tree(world→rmf_obelix/base_link→…→velodyne);原仿真由 RotorS 发。消费 /wm/* 时须由桥侧补:ROS1 小节点从 /wm/odom 广播 world→base_link + 静态 base_link→velodyne。
4. **工期观(Codex)**:可信闭环 4-10 天,科研级 2-4 周。**不过早宣布"集成完成"**——当前准确口径(2026-07-07 中午)=**transport+消费闭环+dry-run+3D 数据链+Stage4 FCU 闭环(4a/b/c)+Stage5a gate 机制全部贯通**(run8:exploration gate 首次以 strategy=gbplanner 过 exploration_probe,accepted_goals=4 全运动到达;上游 EKF 参考系真 bug 已根治,clean 99bcfa1,诊断见 runbooks/world-model-jazzy/stage5a_diagnosis.md);**尚未完成**=TASK_STATUS_OK 全绿(frame_contract/探索质量 run 间波动)、Stage5b"行为随 3D 输入变化"对照、Stage5c 同口径多 run 对比(成功率统计归此)。
5. **证据定性纪律(勿混)**:`stage2k_evidence`=**完整原仿真栈**里的反例(GBPlanner 吃原生 topic,/wm/\* 无订阅者)——**不能**拿它证明消费闭环;消费闭环的正证据=`stage26_evidence`(纯 planner 栈,订阅关系+建图+出轨迹)。
6. **ROS2 路线复核(Review_017 §5,2026-07-07 联网核查)**:**未发现作者官方发布的 ROS2 版 GBPlanner;当前官方可复现路线仍是 ROS1/catkin。** 依据:GitHub API 列举 `ntnu-arl/gbplanner_ros` 全部 16 个分支(master、gbplanner1/2/3、dev/noetic、dev/subt 等)均为 ROS1 系,**无任何 ros2/humble/jazzy/foxy/rclcpp/colcon 命名分支,tag/release 为空**;NTNU 新的 unified_autonomy_stack 文档亦无明确 ROS2 GBPlanner 移植声明;搜索所见第三方 fork(Nimisha-Pabbichetty、chengwei920412 等)均为镜像性 fork 而非 ROS2 移植,**不直接采用**。结论:短期维持 B2.5 薄桥路线;ROS2-native 若做属新工程(算法核心/地图接口/ROS2 外壳三层拆分,须留 ROS1 版作 oracle 回归),不进当前验收路径。替代算法(FUEL/TARE/RACER/nvblox 等)仅作报告"预研"一页,不换主线。

## 六、B16 修改的后果评估(用户要求:确保物理自洽、不为全绿而全绿)

| 改动 | 影响面 | 物理/安全评估 |
|---|---|---|
| 探针订阅 QoS 改 publisher 内省 | 仅诊断探针(只读观察者) | ✅ 让"检测仪表"用正确模式读数,不改变任何被测系统行为;latched 话题本该这么订 |
| 探针等待预算 2s→45s、容器 30→90s | 仅探针窗口与 run 总时长(+~30s) | ✅ 不触碰飞控/EKF/传感器任何参数;等待更久=测量更完整;与 exploration_probe 既有 45s/90s 先例一致 |
| 测试断言更新(slam_test/runtime_artifacts_test) | 仅测试 | ✅ 断言追上 79643b9 已有修复,并加回归守卫(IMU 不自吞、旧参数名不复活) |

**明确不做的**:不改 DDS 发现机制本身(困难 a,micro-ROS agent 上游行为,绕道等待而非篡改)、不加任何飞行参数 hack。

## 七、困难清单结论(2026-07-06 晚,按用户指示逐项)

| # | 困难 | 结论 |
|---|---|---|
| a | DDS 慢发现根源(agent 侧) | **不动**(用户同意):不影响端到端,探针等待预算已覆盖 |
| b | `slam.ready=False` | ✅ **查清,无隐患**:rosbag 时间线实测 t≈215s 起 `ready=True`(scan 115/imu 14841/tf 3103,直到结束)——是 gate/探针**启动早期采样**的口径问题,SLAM 全程健康。证据:`slam_status_timeline.py` on run `130626` |
| c | 探索结果波动(accepted_goals 2~3) | ✅ **根因锁定(源码级)**:`goal_index = ready_elapsed/(window/3)` 纯时间驱动(exploration_workflow_runtime.py.tmpl L78-108),数满 3 目标需 ready 后 ≥17.3s,而 window 仅 26s——**启动耗时(EKF收敛/takeoff时刻)蚕食窗口**。这同时是 frontier_lite"计时器驱动不看地图"的又一实锤。若要提高全绿率可在任务配置层加长 duration(合理配置,非 hack),待用户定 |
| d | 3D 点云(最大价值点) | ✅ **方案锁定官方组件**:ardupilot_gz `lidar_3d`(360×60线)+ 预留桥路 `/lidar/points`→`cloud_in`,见选型 A1' |
| e | ROS1↔jazzy 鸿沟 | ✅ **调研+实验闭环**:官方 foxy 桥(RMW bad_alloc)与 zenoh(rosrust mismatch)双双判死,自写薄桥 transport 三段 PASS,遗留 TCP 稳定性(阶段2.5) |

> **[CURRENT] 路线切换权威记录(2026-07-07 晚,导师最高指示)。此后所有文档口径以本文+CURRENT_STATUS 为准。**

# 路线切换:放弃桥接,GBPlanner 迁移 ROS2 原生

## 一、指示与理由

导师指示(2026-07-07):**一定要把 GBPlanner 算法转移到 ROS2 系统,彻底抛弃桥接方案**。
理由:ROS1+ROS2 两套系统同时跑过于笨重(两个 ROS 栈、TCP 桥、双容器生态、
DDS/XmlRpc 双重脆弱面——今晚演示中 RViz XmlRpc 崩、探针 DDS 匹配退化均为佐证)。

## 二、桥接线的处置(冻结,不清零)

桥接方案(B2.5)定性为**已完成的过渡阶段**,冻结为两个用途:
1. **Oracle 回归基准**:ROS1 原版 GBPlanner(gbplanner_ref 镜像)+ 桥接联跑证据链
   (4c 归因/5a gate/5b 3D 对照/5c 对比)保留,ROS2 版行为等价性对照全靠它;
2. **科研叙事素材**:公平对比(GBPlanner 50% vs frontier_lite 0%)、三大鸿沟、
   失真补证等结论仍然成立——它们证明的是"GBPlanner 算法在 world-model 中有效",
   与运载方式(桥/原生)无关。
桥接代码(thinbridge/gbplanner_ref 容器编排)不再演进,不再修 bug。

## 三、资产盘点:什么直接带走

| 资产 | 处置 |
|---|---|
| world-model 上游修复(EKF 参考系 3 件套、探针预算 4 连修、B1~B16)clean 分支 6 commit | **原样复用**(与桥接无关,是平台修复) |
| ROS2 适配器 trajectory_to_intent_stage4.py(rclpy) | **原样复用**(输入 /gbp/trajectory 改由 ROS2 planner 直出;坐标语义需重估,见契约文档) |
| lidar3d 净增量 + external 策略 | **原样复用** |
| 评测口径与 harness(stage5c_run 系,去桥段)| **改造复用** |
| 公平对比/5b/5c 数据 | 冻结为"桥接期结论",ROS2 版复测后出新表 |
| thinbridge 两侧 / TCP / gbplanner_ref 编排 | 冻结(oracle 用),不演进 |

## 四、迁移蓝图(三层拆分,ROS1 版为 oracle)

```text
A 算法核心层  rrg/gain/raycast/geometry —— 纯 C++,目标:零 ROS 依赖直接编译
B 地图接口层  voxblox —— 选型进行中(官方无 ROS2 版;社区 fork/nvblox/自移植,行为等价优先)
C ROS2 外壳层 rclcpp 节点 + 参数 + 服务;PCI 预判可弃(由薄壳+适配器直连替代,契约文档定)
D 消息层      planner_msgs → ROS2 msg/srv 重定义
E 仿真专用    rotors/rmf_sim 等 —— 弃(world-model 就是仿真环境)
```

里程碑:
- **M0** 路线切换记录+侦察(本文+三份侦察文档)✅ 进行中
- **M1** ros2_ws 布局 + planner_msgs ROS2 化(编译过)
- **M2** voxblox ROS2 路线落地(tsdf/esdf server 在 jazzy 吃 lidar3d 点云建图,与 oracle 同输入对比体素统计)
- **M3** 算法核心剥离编译(A 层零 ROS 依赖,单测对齐 oracle:同一张图上 gain/raycast 数值一致)
- **M4** ROS2 外壳:gbplanner_node(rclcpp)+ 触发循环(替代 PCI),输出 /gbp/trajectory
- **M5** world-model 直连联跑:同 stage5 口径(external+适配器+gate),oracle 回归对照,
  出 ROS2 版的 5c 统计与公平对比复测

验收纪律不变:每步实测证据文件;行为等价用 oracle 数值对齐;不为编译过而阉割语义。

## 五、风险(沿用 Review_017 §4.3,须逐条对照)

voxblox 语义变化 / raycast+gain 统计漂移 / TF 树差异改变采样可达性 /
PCI 行为难无损复刻(→用"薄壳+适配器"绕开而非复刻)/ 原参数在新分辨率下失配。
对策:每层出对齐实验,oracle 在手。

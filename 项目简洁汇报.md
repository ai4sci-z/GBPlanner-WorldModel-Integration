# GBPlanner → world-model 集成:项目简洁汇报(2026-07-05)

> 一页看懂:任务是什么、干到哪了、有什么实打实的成果、下一步。详版见 [README.md](README.md)。

## 一、任务(一句话)
把学术界成熟的 **GBPlanner 自主探索算法**(DARPA 地下赛冠军队 CERBERUS,原始论文 Dang et al. JFR 2020)接进 **world-model** 无人机仿真平台,替换它现在那个"只会按计时器循环三个写死动作、根本不看地图"的占位探索 `frontier_lite`,并用数据证明升级的价值。

## 二、进度总览(五条线)
```
预研B·复现GBPlanner   ██████████ 100% ✅  自主探索全闭环+量化(实测)
预研A·跑通world-model █████████░  ~92%    39轮实验修15坑;FCU已解锁(armed);takeoff卡EKF深水区
jazzy全栈重建(主线)   ████████░░   8/9 ◉  作者环境=jazzy,PR必须jazzy跑通;差official-baseline(构建中)
集成·决策层+桥接地基  ██████░░░░  ~60%    gbplanner_gain原型✅+真版桥接契约✅
量化对比              ███░░░░░░░  ~30%    GBPlanner侧数据✅;frontier_lite侧等jazzy通
```

## 三、实打实的成果(可当场验证,非空口)

1. **✅ 官方 GBPlanner 仿真在本机跑通并量化**(预研B):无人机起飞→3D 建图→图搜索选路→自主巡飞→时间预算到自动返航。**实测:飞行 291.3m、3D 地图 132,091 体素、自动返航、地图落盘 4MB**(曲线 `images/exploration_metrics_full.png`)。两条命令可复现([实操手册](docs/实跑操作手册_图文版.md))。

2. **✅ 论证了 frontier_lite 的本质缺陷**(代码铁证):它不订阅任何地图、按计时器循环 3 个写死动作,`source` 字段自承 `bounded_lite_pattern`——**不是探索算法**。GBPlanner 的精确插入点已锁定。

3. **✅ world-model 平台在本机基本复现 + 修了 13 个真 bug**(预研A):9/9 镜像构建成功;35 轮受控实验逐个剥掉 humble 迁移的 13 个坑(总根因=机器人根本没生成、编排真凶=容器身份致 gz 发现瘫痪…),把平台从"机器人不存在"推进到"感知全通→SLAM 闭环→位姿回灌飞控"。全过程有完整证据链([战役实录](docs/预研A排错战役实录_35轮实验全解.md))。

4. **✅ 集成代码接进真实结构**:`gbplanner_gain` 可选策略(读地图选向)已接进 world-model；真版桥接方案(ros1_bridge)的 I/O 契约从源码逐条证实、ROS2 适配器已写。

5. **✅ 算法核心可运行 + 论文对应**:纯 C++ 体积增益决策 demo(30秒复现);读了原始论文校准了理解、写了[论文↔代码对应关系](docs/GBPlanner原始论文与代码对应关系.md)。

## 四、当前卡点(诚实)
humble 上已推进到 **FCU 解锁成功(armed:true,坑#14 GUIDED 模式映射修好实测)**,卡在最后的 takeoff 被拒(坑#15,EKF 位置源深水区)。**战略决策:不在 humble 死磕**——作者设计环境是 **jazzy**(铁证:上游 config.toml),这些深水坑在 jazzy 可能天然不存在,且 **PR 必须在 jazzy 跑通作者才能用**。故主线转 jazzy 全栈重建:9 镜像已建 8(编译坑全解:Livox cstdint、ydlidar declare_parameter、编排器 BuildKit 假成功),差 official-baseline(构建中)。

## 五、下一步(4 步到终点)
1. jazzy 9/9 → jazzy 上跑通 exploration(frontier_lite 真实指标)→ 2. gbplanner_gain 同口径替换对比 → 3. 完整桥接接原版 3D GBPlanner → 4. **提交 PR**(政策更正 07-05:做完必须提交,自己账号 fork;硬约束=jazzy 兼容,评估见 docs/PR兼容性与jazzy评估.md)。

## 六、诚实边界(不粉饰)
- `gbplanner_gain` 是 **2D 单步**原型,**不是**完整 GBPlanner(3D+图搜索+全局层)——真集成靠桥接;详见论文对应文档的失真标注。
- world-model 端到端**尚未全绿**(humble 差 takeoff;jazzy 全栈在建),现在**不适合现场演示**;可演示的是预研B 的 GBPlanner 仿真(确定能跑)+ 算法 demo。
- 给 world-model 的 12 个提交:代码级 jazzy 兼容已逐条审查+语言层实测,但**完整 jazzy 端到端跑通尚未完成**(正在做,这正是提 PR 前的最后把关)。

# 接力棒 · 当前值班（动手前先读，收工时更新）

> 本文件只保留一个当前交接块。历史轮次在
> [docs/archive/接力棒_历史轮次_截至2026-07-16.md](docs/archive/接力棒_历史轮次_截至2026-07-16.md)。
> 状态一律以 [CURRENT_STATUS.md](CURRENT_STATUS.md) 为准。

## 当前执行者与范围

- 当前执行者：Codex。
- 当前工作包：P0 接管收口 -> P1 真 GBPlanner 短闭环准备。
- 用户已授权修复 NVIDIA 环境并允许必要时重启；当前 GPU 链路已恢复，未触发重启条件。
- 本轮不是正式编号 Review，不创建或更新 `Review_NNN`。

## 固定基线

- 治理主仓：`/home/ai4s/projects/GBPlanner-WorldModel-Integration`，`main`。
- ROS 2 迁移仓：`/home/ai4s/projects/gbp-feat`，
  `feat/gbplanner-ros2-port@ef4f96fcbffa678ec233fa495b7e2162eddf78a4`，与 `origin` 对齐。
- 仿真平台：`/home/ai4s/projects/world-model`，
  `fix/world-model-e2e-takeoff@8649ae553e0b433b867c301b05ca780d49cd2530`；与 `backup` 对齐。
- world-model 原始 GitHub 平台仓：`SZ-surveying/world-model`；授权修复镜像：
  `ai4sci-z/world-model`。只向授权镜像推送，禁止向 SZ-surveying 上游推送。

## 2026-08-24 已完成

- GitHub 线上分支、Tag、Release 与最近提交已直接核验，无遗漏更新。
- 宿主 `nvidia-smi` 和 official-baseline 容器 `--gpus all` 冒烟通过。
- 当前 `ef4f96f` 依赖链在固定 `voxblox_ros2_deps:jazzy` 镜像中 11 包构建通过。
- `gbplanner_core` gtest 4/4 通过。
- Python 适配器测试 17/17 通过；M4 真 RRG 合成冒烟输出 13 航点。
- `world-model@8649ae5` 的 `go test ./...` 全模块通过，正式 `exploration_probe` 已恢复为
  required，且 required/optional 与 mission 分类均有回归测试。
- 统一验证证据：`/home/ai4s/aa_runs/gbplanner_verify/20260824T031820Z`，`VERIFY_RC=0`。
- 历史 PCL 缺头文件定位为误用 official-baseline 运行镜像，不是源码 include 错误。
- 桌面入口已把真 GBPlanner 与 2D 增益代理演示分轨，并移除“删除全部运行容器”的清理逻辑。

## 当前停点

1. 完成 P0 claim/manifest 双提交闭包并推送治理仓。
2. 固定三仓 SHA 后运行 P1-2 短时真 GBPlanner 闭环。
3. 只有同一 run 正常收尾、最终 summary 存在且 `ok=true`，并同时具备
   RRG、voxblox、trajectory、adapter、FCU 证据，短跑才判 PASS。
4. 短跑 PASS 后才进入 10/10；之后才进入长稳。P2/P3/P4 继续阻塞。

## 关键命令

```bash
cd /home/ai4s/projects/GBPlanner-WorldModel-Integration
python3 governance/doc_closure_check.py
bash governance/test_generate_manifest.sh
```

真 M5 入口：

```bash
cd /home/ai4s/projects/gbp-feat
bash runbooks/ros2_port/m5_direct_run.sh
```

该入口必须使用上述固定 `gbp-feat` 和 `world-model` SHA；2D 代理不得进入验收分母。

## 证据边界

- `20260730T033541.154997958Z` 是未正常收尾的长跑，不能判 PASS。
- `20260731T065059.411462422Z` 是 NVML 失败，GPU 当前恢复不改变历史结论。
- M4 11 航点证明真核心在合成输入下可调用，不证明 world-model 飞行闭环或 3D 等价。
- 2D 代理的 `accepted_goals/path_length` 只属于演示线，不进入真 GBPlanner 分母。

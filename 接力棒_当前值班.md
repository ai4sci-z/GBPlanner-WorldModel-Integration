# 接力棒 · 当前值班(动手前先读,收工时更新)

> 规则见 `双Agent交替协作协议.md`。这文件是"锁":谁开工先读它,别碰别人锁定区;谁收工更新它。

---
**当前值班执行棒**: Claude Code(2026-07-13,原生 Linux 新环境)

---
## 🖥️ 2026-07-13:WSL → 原生 Ubuntu 24.04 迁移(HANDOVER_LINUX.md §0 接管协议执行中)

**环境**: Ubuntu 24.04.4 / 24 核 / 30G / 1.3T / RTX5060。本仓 `/home/ai4s/projects/GBPlanner-WorldModel-Integration`;
world-model `/home/ai4s/projects/world-model`(clean 分支);ardupilot_gz `/home/ai4s/projects/ardupilot_gz`(@8df4dc1,同级,必需)。
feat 分支挂在 git worktree `/home/ai4s/projects/gbp-feat`。构建日志 `~/build-logs-jazzy/`。

**验收门状态**:

| 门 | 内容 | 状态 |
|---|---|---|
| GATE-0 | docker engine + go≥1.24 | ✅ docker 29.6.1 / go 1.26.5(`runbooks/migration/bootstrap_linux_gate0.sh` 一键) |
| GATE-1 | ros:jazzy-ros-base | ✅ |
| GATE-2 | navlab 9 镜像 | ✅ **9/9**(`runbooks/world-model-jazzy/build_all_jazzy_native.sh`) |
| GATE-3 | go build + go test | ✅ 三包全绿(tasks 包未见 flaky) |
| GATE-5 | voxblox_ros2_deps:jazzy | ✅ |
| GATE-6 | M2 复验 colcon+gtest | ✅ **COLCON_RC=0 + gtest 10/10**(`runbooks/ros2_port/m2_verify_native.sh`) |
| GATE-7 | gbplanner-ref oracle 镜像 | ✅ 10.7GB,开箱验真(gbplanner_node/libgbplanner.so/voxblox 三库/noetic) |
| **GATE-4** | **live run 全绿** | ✅ **TASK_STATUS_OK 全绿**(run `20260713T100104`,双死锁修复后;证据 `runbooks/world-model-jazzy/gate4_native_pass_evidence.txt`) |

**迁移中发现的坑(交接文档没写的)**:
1. **bundle 丢了两个修复**(只带了 main+clean 分支,镜像层修复提交在旧 WSL 的另一分支):
   `68c19bf` fast-lio cstdint / `12ab9f0` 坑#8 QoS(台账 B9)。已按记录重做 → clean `a05e20b` + `43da557`。
   (顺带:**有害**的 `d8ff119` 也不在 bundle → gazebo-sensor 可用 in-repo Dockerfile)
2. **`third_party/` 是 8 个 submodule,clone 后全空** → 必须 `git submodule update --init --recursive`(1.3G)。
3. **`ardupilot_gz` 必须 clone 到 world-model 同级**(检查单第 28 行有记,HANDOVER §3 漏了)。
4. **voxblox `mesh_results/.keep` 被 .gitignore 静默吃掉**(vendor 时的雷,WSL 侧目录碰巧还在所以没炸)
   → 已 `git add -f` 找回(feat `0abe6eb`),GATE-6 随即全绿。
5. **official-baseline 的 7897 代理是必需的**(我一度以为是 WSL 遗留而删掉 → 直连克隆 ArduPilot 90s 被 TLS 掐断)。
   已实证:构建期代理**不会固化进镜像**,换网络无需重建(`runbooks/migration/proxy_persistence_evidence.txt`)。
6. **ydlidar 坑②在 jazzy 原样重现** → gazebo-sensor 须用 `runbooks/world-model-jazzy/gazebo-sensor-jazzy.Dockerfile`。

**✅ GATE-4 已收口(2026-07-13 傍晚,双死锁根因链全实测定案)**:飞机从未起飞的真相是**两个叠加的死锁**,
全链证据见 `runbooks/world-model-jazzy/gate4_native_pass_evidence.txt`:
- **死锁①(环境级)**:容器内 glvnd 把 EGL 路由到 Mesa → libgallium `driCreateNewScreen3` 段错误 →
  gz-sim 整体死亡 → ArduPilotPlugin 未绑 9002 → SITL 锁步循环等待 → 零心跳。
  修复 = 宿主 nvidia-container-toolkit(default-runtime=nvidia)+ official-baseline 容器三 env
  (NVIDIA_VISIBLE_DEVICES/CAPABILITIES + `__EGL_VENDOR_LIBRARY_FILENAMES` 钉 NVIDIA)→ clean `79cbb9f`。
- **死锁②(上游真 bug,B15 同族,拟入台账 B17)**:companion 外部导航(sysid191,GCS 型)心跳比飞控早 296ms
  到 14551(tcpdump 实证),pymavlink 对 GCS 心跳不设 target → `target_system=0` 广播,mode/arm/takeoff
  全被无视。WSL 上飞控总赢竞态故从未暴露。修复 = bootstrap 等到 `target_system!=0` → clean `8df2690`。
- ⚠️ 早前"ArduPilot master 版本漂移"嫌疑已**证伪**(07-05→07-13 无 Copter/JSON 链路改动)——勿再追。
  但 `ARG ARDUPILOT_REF=master` 未 pin commit 的**可重现性风险仍真实存在**,列为待办(pin 具体 commit)。

**✅ 顺带实证**:benewake TFmini emulator 在本次 run 里 frame_count=1839 正常收数 → 重做的 QoS 修复(B9)确实生效。

**M2 切片5 前置已做**:`compare_layers.py` 已支持 ESDF 层(feat `15c8969`)——EsdfVoxel 格式从源码定案 =
**2×uint32**(`| f32 distance | 3×int8 parent | 8b flags |`,flags bit0=observed,**没有 weight 字段**,
判观测的口径与 TSDF 不同);TSDF 路径用库内真实层文件自对拍回归 PASS(133,429 体素 RMS=0)。
诚实边界:ESDF 路径尚无真实层文件实测,格式来自源码而非实跑。

---

**(以下为 2026-07-08 WSL 侧状态,保留追溯)**

**在干什么**: 🔄 **路线已切换(07-07 晚,导师最高指示):放弃桥接,GBPlanner 迁移 ROS2 原生**。当前 = **M0 入口文档收口**——README/TASKS/文档索引已切 ROS2 主线 ✅、`docs/worldmodel理解_0_总览_给ROS2迁移.md` 已补 ✅。

**M0 当前施工点(下一棒四件事,状态实时)**:
1. ✅ **stage6 修⑥+⑦ 全收口,live 全绿观测达成(07-08 傍晚)**:修⑥(clean `ff24087`,frame_contract FCUPoseTopic default→消费链路 `/navlab/fcu/local_position_pose`)live 验证 ok=True;复跑时揪出**同病根第二器官=修⑦**(clean `050ee94`):`helpers/rosbag_topic_sets.go` ExplorationTaskRequiredTopics 仍硬性要求 `/ap/v1/pose+twist/filtered` 两条 micro-ROS flaky 调试流(实测 bag 缺席→required_topics_missing→整跑 BLOCKED),已换消费链路 pose(录制清单不动,Navigation 同款问题记欠账未动)。**修⑦后首跑 20260708T101402 = TASK_STATUS_OK 完整全绿**(blockers=[];证据 runbooks/world-model-jazzy/stage6_live_evidence.txt)。**上窗口"冷启动环境失败"翻案**:真因=stage6_live.sh 用 `--artifact-root /tmp`(workspace 外)→ docker 探针容器按 workspace 前缀映射输出路径必挂;**--artifact-root 铁律:只能指 workspace 内**。
2. ✅ 补 `docs/worldmodel理解_0_总览_给ROS2迁移.md`(本轮完成)。
3. ✅ 入口文档收口(本轮 README/TASKS/文档索引 全切 ROS2 主线)。
4. ✅ **M1 完成(07-08)**:分支 `feat/gbplanner-ros2-port`(已推送,commit e343421)+ `ros2_port/src/planner_msgs` 最小 8 接口;jazzy 容器 colcon build rc=0 + `ros2 interface show` 8/8 全注册(证据 runbooks/ros2_port/m1_build_evidence.txt;复现 m1_build.sh)。**踩坑记录**:rosidl 强制接口文件 PascalCase + 常量 UPPER_SNAKE_CASE(kCamelCase 直接报错),差异全标注 ros2_port/README.md。**下一棒=M2 voxblox ROS2 底座**(snt-arg minimal + Jazzy 适配 + ROS1 oracle 对拍,任务书 §7-M2)。
5. 🔵 **M2 切片 1-3 完成(07-08 晚,feat 分支三连 commit d8b4ee1/3018307/aa66fcc 已推)**:①vendor snt-arg@d08e9d4 + Jazzy 全量 9/9 构建 ✅ ②维护补丁(幽灵依赖/gflags 参数管道,E2E param 回读实证)✅ ③ntnu dev/noetic tsdf_integrator 行为等价补丁(**gtest 10/10**;sparsity×100 陷阱拆除)✅。**接管必读=ros2_port/README.md §M2 + ros2_port/docs/m2_scout_{1,2,3}**(三路侦察:最小集 7 包/ntnu 定制收敛面/Jazzy 配方;GBPlanner 实 pin dev/noetic 分支)。**切片4 ✅ oracle 逐体素对拍双积分器全 PASS(1f61ff1)**:harness=ros2_port/oracle_cmp/(输入逐字节一致 SHA 互证);simple 零差(35,323/35,323)、fast mismatch 2/34,506;路上修真移植 bug=min_time 节流 1s 无操作(from_seconds 静态工厂误用)+ 纠正 ROS2 订阅名为私有名 /voxblox_node/pointcloud(侦察报告①此点有误)。**剩切片5=ESDF 对拍+world-model 场景建图+RViz2**。**⭐ 原生 Linux 接管引导已就绪 = [HANDOVER_LINUX.md](HANDOVER_LINUX.md)**(新环境 Claude Code 读它即无损接管:状态快照/环境重建验收门/规则三分类/任务队列/记忆重建)。**另:用户已拍板新盘装原生 Linux**——迁移检查单 docs/原生Linux迁移检查单_2026-07-08.md 已入库,world-model clean 分支 bundle 保险已落 C:\CCproject\backups\;建议时机=M2 收口后干净断点(WSL 当前无阻塞,重建实账≈半天、动手 1-1.5h)。

详见 [TASKS.md](TASKS.md) M0-M5 与 [任务书](docs/GBPlanner_ROS2原生迁移可行性与任务拆解_2026-07-08.md)、[理解总览](docs/worldmodel理解_0_总览_给ROS2迁移.md)。

**锁定区提醒**:WSL `~/ws-clean/world-model`(clean 分支)有 stage6 修⑤⑥未提交改动(3 文件),提交脚本 `runbooks/world-model-jazzy/stage6_clean_commit.sh`。

---

**以下为桥接期战果(已冻结为 oracle,仅追溯,不再演进)**:🏁 world-model exploration 已端到端全绿(run `20260706T130626`,TASK_STATUS_OK/4探针全ok/3目标/物理起飞SIM+0.72m,无hack;B15死锁+B16探针双根因已修,详见 Bug台账)。桥接主线 B2.5 分阶段实测:
- 阶段0 ✅ 基线定档(6 run:全绿2/6、达标率40%、根因=启动耗时蚕食26s探索窗口,见 docs/基线定档)
- 阶段1 ✅ gbplanner-ref 单侧复验(trajectory/frames 实测,stage1_evidence)
- 阶段2 ✅ PASS(stage2j):transport 三段通(**jazzy `ros2 topic echo` CLI 对 rclpy 发布者收不到=工具坑,验收一律 rclpy 订阅**,stage2i);官方 foxy 桥与 zenoh 均实验判死
- 阶段2.5 ✅:TCP 断连双根因修复(心跳防空闲掐断 + acceptor timeout break 致死→continue)
- **阶段2.6 ✅ 消费闭环打通(stage26_evidence,里程碑)**:纯 planner 栈(wm_planner.launch)+ 薄桥 TF 广播;三级验收全过——订阅关系指向 /wm/*、voxblox 2.303Hz 建图、**19 条 trajectory 出到 /gbp/trajectory**。诚实边界:输入仍 2D 冒烟(z=0)
- **阶段3 ✅ dry-run PASS(stage3_evidence)**:traj_dryrun.py 零发布,37 条跟踪量全自洽;2D 输入下 waypoint 已见 z 抬升。语义:PCI 循环靠"轨迹被执行"驱动,静止 odom 挂起循环→持续探索需阶段4 FCU 闭环
- **阶段3.5 ✅ 3D 数据链贯通(stage35_evidence,00:01)**:lidar3d 净增量(z 跨度 3.3m)→ 薄桥 cloud3d 直通(base64/2Hz,3D 优先)→ **史诗同框**:world-model 真栈 odom 604 条+3D 点云 115 帧喂 GBPlanner → **voxblox 3D 体素 90,472 点 zspan=13.2m(判据②)** → trajectory 回流(判据③初步,z 分量存在,大机动待 Stage4)
- **阶段4a/4b/4c ✅ 全过**:消费直证(GBP-SIGNATURE 逐位吻合)+ **4c 去混流可归因**(stage4c_external_evidence:frontier intent=0、GBP 运动 intent 61、签名 cmd_vel 14、签名窗口 odom path 0.99m≥0.10m、takeoff ok)
- **阶段5a ✅ 机制验收 PASS(run8 `20260707T041455`,07-07 中午)**:exploration gate **首次以 strategy=gbplanner 通过 exploration_probe**(适配器五条件闩锁:GATE OK wp_done=4 全运动到达/预到达剔除,path 0.80m,blockers=[],probe 采样 ok=True rc=0)。**路上根治上游 EKF 参考系打架真 bug**(EK3 罗盘 yaw vs SLAM 位置差 δ→运动即 stopped aiding→position lost→跑飞 34m=基线 path 方差根因;修复 3 件套已提交 **clean 分支 99bcfa1**;八跑迭代全程 BIN 验尸=**runbooks/world-model-jazzy/stage5a_diagnosis.md** 必读)。适配器最终形态:Procrustes 在线坐标对齐+yaw_rate 恒 0+slam_frozen blocker+近距比例减速+诚实 wp 计数+混流闩锁。**诚实边界**:全绿 TASK_STATUS_OK 未达成(run8 卡 frame_contract 波动;run9 探索质量波动 accepted=1)→ 成功率统计归 5c
- **阶段5b ✅ 3D 行为对照成立(07-07 下午,stage5b_evidence)**:同参数只改 lidar3d 垂直 FOV(±30°→±5°),输入云 zspan 9.08→0.48、TSDF 点数 121k→54k、trajectory zspan 收缩一个量级+重规划模式改变。**意外收获:变体 A=GBPlanner 策略下首个 TASK_STATUS_OK 完整全绿 run(runA)**
- **阶段5c ✅ 首批 6 样本定档(stage5c_summary_evidence)**:gate 达标率 3/6=50%(vs 基线 40%,我方 accepted=真实运动到达)、全绿 1/6、path 方差 0.30(基线跨 3.4);失败=A类探索质量 3 次+B类 frame_contract 波动;失真补证表:cmd_vel↔intent 方向差 0~14°=FCU 转发忠实。⚠️基线跑于 EKF 修复前,严格对比需重跑
- **ROS2 复核(Review_017 §5)✅**:未发现官方 ROS2 版 GBPlanner(ntnu-arl 16 分支全 ROS1、0 tag);短期维持 B2.5 薄桥
- **A类主修 ✅ 适配器 v5(外环 PD 阻尼)**:run3 验尸=P-only 极限环 ±0.3m 套不住 0.15m 捕获圈;v5 后 v2run1 wp 捕获 **0→9** 直证有效;latch 时机(晚于探针窗口)是下一杠杆;全绿率结论须 v2 系统性批跑,单跑不算数
- **🏆 基线修复后复档 ✅(baseline_postfix_evidence)**:frontier_lite 同口径 6 run **全部 FAIL,accepted 恒=2 零方差**(窗口结构性失败;修复前 2/6 全绿=EKF 跑飞馈赠)→ **公平对比:GBPlanner 达标 50% vs 基线 0%,显著占优**
- **v2 系统性批跑 ✅ 定档(stage5c_v2_summary_evidence,冻结口径 5a506fe,6 run)**:gate 达标 1/6=17%(首批 50%)、全绿 0/6——**v5 未提升统计**(诚实记录);失败=A类4/B类1/**C类·latch 晚于 probe 采样窗口 1(新类别,v2run5)**;合并 12 run 达标 33%,仍显著优于基线 0/6,公平对比主结论不变。下一杠杆:①**读 runner 源码定 exploration_probe 采样时刻**(C类确定解,盲调不如先查)②回退 kp0.35/kd0.5 对照批(0→9 直证参数)③bootstrap_ready 即触发规划
- **文档收口 ✅(Review_019)**:README 重写(融合定性+链路表+三大鸿沟);对比实验改结果文档(§0 公平对比主表);桥接计划收口至 5c/baseline_postfix/v5+去身份痕迹;TASKS §四更新
- **成功率战役 ✅(07-07 组会前,stage5d_success_evidence 必读)**:C/B 类根因全链修复(探针预算 35→90/容器 150/类型发现全预算/frame_contract 45→90/探针瘦身;clean 分支 e7ca9fc)+ 适配器 v6b(累计位移基线法,kp0.35);修复后 gate 达标连续段,**final2 再次 TASK_STATUS_OK 全绿(accepted=5)**;⚠️最终口径正式批仅 3 样本(组会中断),成功率未定档
- **GUI 三演示 ✅ 已交付(阶段性可视化口径)**:总控 gui_demo_master.sh(桥栈+external live+Gazebo GUI+RViz)/gui/results_panel.html(离线结果面板)/gui/组会演示手册(图例+讲稿+fallback);RViz 实测弹窗
- ~~当前=组会后:①最终口径 full 批 ②基线同口径重跑 ③桥接 PR 定稿~~ → **⛔ 已冻结**(路线切换后不再执行;当前主线见本文件顶部 M0-M5)
- **文档体系已重整(2026-07-06 深夜)**:唯一事实源=CURRENT_STATUS.md;README 重写为入口页;12 份历史文档归档 docs/archive/;全库状态标签(CURRENT/ACTIVE/DRAFT/REFERENCE/HISTORICAL/OBSOLETE)

**锁定区(别人别碰)**: WSL `~/ws-clean/world-model`(clean分支4 commit)+ docker + `runbooks/world-model-jazzy/` + `integration/ros1_bridge/` + `README/RESUME/TASKS/Bug台账`。

**PR 政策**: ⏸ 延后(用户 2026-07-06 晚指示:源码改动先保存在 clean 分支,**等最终桥接集成跑通后统一定稿 PR 物料**)。PR_BODY/ISSUE_BODY 为 DRAFT。

**下一棒从哪继续**: **[CURRENT_STATUS.md](CURRENT_STATUS.md)(唯一事实源)** + [TASKS.md](TASKS.md) + 本文件 + [docs/桥接查证与执行计划_2026-07-06.md](docs/桥接查证与执行计划_2026-07-06.md);工具铁律见 RESUME_新窗口接管(已薄化,只剩铁律)。

- **🔄 路线切换(07-07 晚,导师最高指示)**:放弃桥接,GBPlanner 迁移 ROS2 原生(docs/路线切换_ROS2迁移_2026-07-07.md)。桥接线冻结=oracle+素材;世界模型侧资产全复用。**M0 侦察✅三件套**(包清单/voxblox选型=沿用voxblox底座snt-arg minimal/直连契约=PCI可弃+适配器零改动)
- **📋 权威任务拆解已入库**:docs/GBPlanner_ROS2原生迁移可行性与任务拆解_2026-07-08.md(Codex 定稿)——**M1 只迁最小消息集**(planner_srv+PlanningMode 等,勿 13msg+24srv 全家桶)、**新分支 feat/gbplanner-ros2-port + 新目录 ros2_port/ 隔离**、voxblox-first(nvblox 不作第一版)、第一阶段验收=colcon 编译+订 PointCloud2/odom/TF+出 trajectory+RViz2 可见(**不要求 exploration 全绿**)。⚠️入口文档(README/TASKS/文档索引)仍有"主线=B2.5薄桥"旧口径,**M0 收口须先修**
- **🏁 "先跑通 world-model"新工作序(07-08 凌晨)战果**:复验 4 跑——**探索关 4/4 达标**(探针预算修复直接惠及基线!)、**run2=TASK_STATUS_OK 完整全绿**;剩 frame_contract 独苗 flake,已修⑤(publisher-info 重试+tf_static transient_local 回退,已过 go test)、修⑥半成品(FCUPoseTopic 改 /navlab/fcu/local_position_pose——**改在 helpers/runtime_specs.go L459 但渲染未生效**:赋值链=runtime_artifacts.go L722 spec.FCUPoseTopic=frame.FCUPoseTopic ← config/defaults.go 多处 default "/ap/v1/pose/filtered",**下一棒:改 config/defaults.go 里 frame_contract 那条 default+测试同步+复跑验证**)。⚠️ clean 分支有未提交改动(修⑤⑥,3 个文件)
- **📖 world-model 理解报告 3/4 就绪**:docs/worldmodel理解_{1编排生命周期,2ROS图数据流,3探索策略接口}.md(0_总览因会话限额未生成,下一棒补)。已知 flaky:go test tasks 包偶发 FAIL 重跑即过(未定位,记录在案)
- **当前=①clean 分支修⑥收尾+提交 ②补 0_总览 ③M0 入口文档收口(README/TASKS/索引)④新分支 feat/gbplanner-ros2-port + ros2_port/ + M1 最小 planner_msgs**

**更新时间**: 2026-07-08 凌晨(保存上下文:跑通战果+修⑥半成品+迁移拆解入库;下一棒从上行四件事开始)

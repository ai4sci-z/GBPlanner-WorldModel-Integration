# 接力棒 · 当前值班(动手前先读,收工时更新)

> 本文件只保留**一个**当前交接块。历史轮次在
> [docs/archive/接力棒_历史轮次_截至2026-07-16.md](docs/archive/接力棒_历史轮次_截至2026-07-16.md)(只读)。
> 状态一律以 [CURRENT_STATUS.md](CURRENT_STATUS.md) 为准,本文不复制状态。

---
**当前执行者**:Claude Code(det)
**当前 HEAD**:main(见 `governance/manifest_main.tsv` 头 `HEAD=`)
**当前工作包**:WP304 A/A 观测链。机器门链(preflight/aa_cli 四模式/完整分母 aggregate/
防误触授权门/终态三轴门/观测条件门)已建成并经五轮 Codex 击穿-补正,五验包
VERIFIED_PASS;AA001 已执行(产出 OPEN-1 首个受控复现+候选链);AA002=INVALID_OBSERVATION
(样本保留);**真实 AA003 已执行**(2026-07-22,负责人批准 exact SHA=3c41d8e…):**aa-r1 OFF 失败即停**
(~3m44s/preflight_timeout/S1 wait_nav_ready/never arm),aa-r3/aa-r2/aa-r4=NOT_STARTED 入分母;
**A/A 无结论**(ON 未跑);**★OPEN-1 新证据=首个失败样本+BIN**(LOG_DISARMED=1 生效,3.1MB 含
XKF/NKF/VISO,消除 no-BIN 盲区)。判读=[open1/AA003_result_OPEN1判读_2026-07-22.md](runbooks/world-model-jazzy/l0_hover/open1/AA003_result_OPEN1判读_2026-07-22.md)。
机器门/前置记录=[证据文档](governance/AA环境接线_companion镜像重建与preflight复核_2026-07-20.md) §5-§9。

**按序读**:[CURRENT_STATUS.md](CURRENT_STATUS.md) → [TASKS.md](TASKS.md) →
[工作铁律.md](工作铁律.md) → [Bug 台账](docs/world-model端到端Bug台账_给作者PR.md)
→ `~/桌面/ClaudeCode_Reviews/`(R003 系列主令)

**允许/禁止**:管理主仓文档+governance 可改;gbp-feat 只读;world-model 授权链已随
负责人裁决推进至 `6d412a11`(B1 观测改动,已推 backup),**现冻结只读**——再动 wm 须
负责人新授权;不打 tag/改分支/改 remote;上游(SZ-surveying)永不推;
**未获负责人 approval 不得启动 A/A/SITL/Gazebo/ArduPilot/真实任务/P2-LIVE**。

**⚠️OPEN-1 保持 OPEN(2026-07-27 自我校正:此前本会话曾误写"已修复",过度声称,现撤回)**:
候选根因=FCU LOCAL_POSITION_NED 受 RTF=0.30 节流(≈5.5Hz墙钟)× companion 墙钟新鲜度阈值 ×
零迟滞 5s 门 → readiness 狂闪 → preflight_timeout。本会话提交 sim-clock 候选修复 **wm@6c28d26**
(time_boot_ms 判新鲜度,撤回 4000ms 权宜退回 1000;已推 backup,未动上游)——**未验证,不得当已修复**:
①另窗实时审计:demo"7次起飞"仅指 armed+airborne 签名,**8/8 正式 run 仍 TASK_STATUS_BLOCKED**
(降落/观测段),run 级非 PASS;②6c28d26 **零回归测试**,已知反例(链路死→boot_ms 冻结→永久"新鲜";
FCU 重启→max()粘旧→永久 not-fresh)未处置;③本会话默认档 hover run 实测**挂 required probe
frame_contract_probe rc=20、飞机根本没起飞**(新阻塞,WP304→WP307 拦路)。收口须默认 10/10(WP307),现未过。

**⚠️本会话治理破坏(须认领)**:demo 脚本每轮 `docker rm -f` **销毁了待负责人裁决保留的 17 个 AA exited
容器**(AA 样本目录 ~/aa_runs 完好,仅容器实例没了)。已在记忆 [[open1-durable-fix]] 记录审计原文。

**R3-B 诊断已做(2026-07-27)**:frame_contract_probe rc=20 = `topic_sample_missing:/navlab/fcu/local_position_pose`
(探针 90s/899 次零样本,其余话题 /imu /scan /slam/odom /tf 全 ok)。该话题由 external_nav.py:499
**仅在收到 FCU MAVLink LOCAL_POSITION_NED 时发布** → 失败 run 中 **FCU 根本没送 LOCAL_POSITION_NED**。
**这与 OPEN-1 同一根(FCU 局部位置不可靠),是其严重端**:轻微慢→readiness 狂闪 preflight_timeout;
严重缺→probe 零样本 run 起飞前被挡。真根在**上游(FCU/EKF 为何不出 LOCAL_POSITION_NED)仍 UNKNOWN**;
6c28d26 只改下游"判新鲜度",**不触及上游,且其反例(boot_ms 冻结→永久 fresh)恰会掩盖此"LP 停发"情形**。

**R3-D 上游实证(2026-07-27,主线)**:本会话 frame_contract_probe rc=20 的**真根=本会话遗留的
QGroundControl 占用 UDP 14550**→sim 的 mavlink-router `Error binding 0.0.0.0:14550 Address already
in use / Could not open`→收不到 FCU MAVLink→转不到 companion(14553)→`local_position_count=0`→
`/navlab/fcu/local_position_pose` 不发→探针零样本→起飞前被挡。**实证对拍**:关掉 QGC、同配置同代码干净
重跑 `hover`(默认档,headless)→mavlink-router `Opened UDP Server 0.0.0.0:14550`、`local_position_count=750`、
frame_contract_probe `ok=true`、**run=`TASK_STATUS_OK`/`S13 task_success`(首个 run 级全过)**
=`artifacts/sim/hover/20260727T064801`。**严格边界:这是 1 个 run,非 10/10,OPEN-1 不写已关闭;
RTF 慢 LP 狂闪是否仍间歇须多跑验证(下一步 WP307 复现批)。** 加固待办:mavlink-router 绑定失败应硬报错
(现静默拖成 frame_contract),QGC 不得与 sim 同占 14550。

**R3-E 复现批(2026-07-27,干净环境无QGC,默认档×6)**:**PASS 4/6(67%)**,全部 LP 健康(558-2406,
不再饿死)。**剩两种独立残余失败(WP307 10/10 的真拦路,非 QGC、非下游新鲜度)**:
①**run5 解锁失败**=`arm_ack_ok=True 但 armed_seen=False`,一切就绪(external_nav_ready/fcu_local_position_ready/
guided 全 True)、飞控 ACK 了解锁却不真解锁,卡 S3 arm 到 300s 超时→TASK_STATUS_ERROR(飞控侧,真·起不了飞);
②**run1 降落确认门**=飞完 S13 task_success、mission `disarmed=True/landing_ok=True`,验收门却报
`disarm_not_confirmed/motors_not_safe`(证据缺口,飞行本身没问题)。数据=artifacts/.../20260727T1114-1134 六个。
**结论:OPEN-1/起飞稳定性未收口(67%≠100%);QGC 只解释环境端一种模式。**
**run5 tlog 深挖(飞控原话 STATUSTEXT)**:不解锁真因=`Arm: Accels inconsistent`(双IMU加速度计读数超容差,
与 B22 IMU倒装/IMU covariance 债同源)+ `PreArm: VisOdom: not healthy`(ArduPilot 自身 VISO 健康检查在
解锁瞬间未过,虽 companion 报 external_nav_ready=True);序列 "Arming motors→Disarming motors",心跳全程
DISARMED;EKF flags 出现过 0(瞬时全不健康)。**=飞控/传感器层间歇健康问题,归 WP306(IMU)/分层恢复,
非下游补丁可解。** run5 tlog=artifacts/.../20260727T112738/sitl/mav.tlog。
**★×6 tlog 对比深挖(2026-07-27):`Arm: Accels inconsistent` 在全部6个run里都出现 x20(含4个通过的)
=持续系统性、不是偶发噪声;差别只是通过run"Arming→Disarming ×1即成"、run5"×8次次弹回到超时"。
即:两SITL IMU加速度计系统性贴阈值不一致,解锁成败=时机抛硬币,run5 连续8次不走运。**根=系统IMU
加速度计一致性(与 B22 iris IMU roll-180 倒装债同源),修好IMU配置让两IMU一致即消此间歇解锁失败=
~~WP306正活~~。**⚠️2026-07-28 纠正:accels-inconsistent 每 run x20(含全部通过的),与 CURRENT_STATUS §四
"已证伪 Accels inconsistent=判别器"一致——它非判别项;我把 run5 盖章"WP306/IMU"是过度归因。run5 实为
又一个 OPEN-1 类间歇 arm 失败(arm 被接受后又掉 ×8,真判别器仍 UNKNOWN),属 WP304/OPEN-1 调查,不是干净 WP306。**
不 hack 关 ARMING_CHECK。

**R3-H 降落门精确机理(2026-07-27,WP303,可修)**:run1 的 `disarm_not_confirmed/motors_not_safe`
**不在 mission_summary,来自 probes/slam_hover_probe.json**——探针采样 `/navlab/landing/status` 抢在
**触地后强制上锁的 3s 宽限期(force_disarm_grace_sec=3.0)未走完**时快照,抓到 `disarmed=False/
force_disarm_used=False/motors_safe=False` → 经 summary.py:220-223 产出阻塞;而 mission 终态(3s后
force_disarm 完成)是 disarmed=True/land.blockers=[]。**=探针采样时机太早的时序竞态,飞行/降落本身正常。**
修法:探针须在 grace 走完后评估或采信 mission 终态/取最后一帧;须带隔离测试再上(避免再自造回归)。
**★已修+验证(wm@e16479e,推 backup,待提 PR)**:build_landing_summary 视"force-disarm pending"为进行中而非
失败,test-first 4 用例(1主+3守卫不掩盖真失败)红→绿+现有 test_mission_summary 5 无回归。**×8 端到端验证批:
降落门失败(disarm_not_confirmed)一次都没再出现=修复生效**;8/8 起飞、7/8 通过。**露出下一残余=run6
`hover_xy_alignment_direction_mismatch`(external_nav odom vs slam odom XY 方向打架)+`hover_xy_evidence_disagreement`
=导航帧/轴一致性,属 B21/B22 域,是分层恢复下一层**。判据=gate_evaluation.go:1415-1418,阈值 MaxDirectionErrorRad=0.35(≈20°),比较 external_nav/slam/candidate 各源水平运动方向;run6=external_nav candidate(未校正)vs 已校正源方向打架。深层帧一致性,非快修,须专门做。

**R3-I 深挖(2026-07-28):判据有幅值保护**(gate_evaluation.go:1514 `direction_check_ok` 要求两向量幅值≥0.05m,1526 cosine<0.50=方向差>60°才判)——**非悬停噪声误报**。run6 是真移动>5cm 且 external_nav_odom_candidate(未校正)vs external_nav_odom(B21东轴取负校正后)/slam 方向差>60°;因 B21 校正本改方向,该对比较或本身 ill-posed,或 candidate 源真有帧bug。**只有 hover 期 XY 运动>5cm 才触发=1/8。深 B21 帧域,须专门做。**

**R3-G 深挖(2026-07-28,run5 tlog 时间线)**:模式=**Arming motors→停~33s→Disarming→1.3s后重arm→…×8→300s超时**;~33s 无起飞后掉锁=ArduPilot arm-后-未起飞自动上锁(DISARM_DELAY)。**真问题=解锁后 takeoff 没执行、mission 卡 S3 arm 从没进 S4 takeoff**(armed_seen 始终没登记);HEARTBEAT 从没显示 ARMED。**=OPEN-1 在 arm/takeoff 边界的间歇失败,机制更深、判别器仍 UNKNOWN,属 WP304/OPEN-1 长期调查。**
**★R3-G 工程修点定位(2026-07-28,可 test-first)**:控制器 arm 循环(fcu_controller_runtime.py.tmpl:260-303)确认 armed 才过,但 **takeoff 循环(313-352)发 NAV_TAKEOFF 前不重查/不重arm**——飞机在 takeoff 循环里 DISARM_DELAY auto-disarm 掉后,控制器一直对'未解锁飞机'发 takeoff 全被拒到超时(=run5 死循环直接工程因)。**修=takeoff 循环发命令前查 armed,掉了先补 arm(正当鲁棒化,不掩盖真失败)。** 须 test-first(反例:disarm后takeoff被拒→补arm后成)+多轮验证批;上游判别器(为何这run arm瞬间不稳)仍=OPEN-1 UNKNOWN。
**★R3-G 再定位(2026-07-28 验证批打脸)**:补 arm 修复(e58c802)×10 验证=**无效已回退(5a85bd9)**——PASS 6/10(比无修 9/10 还差,变差主因是 R3-G arm 失败 2/10 依旧)。真因:失败 run(run2/run6)卡在 **mission FSM 的 S3 arm(armed_seen 没登记)**,这比控制器 takeoff 循环更上层;**我把修复插在 takeoff 循环=修错层,那些 run 根本没走到那儿。R3-G 的正确修点在 mission armed_seen 登记 / 控制器 arm 循环(非 takeoff 循环),下轮定位这层。** 上游判别器仍 OPEN-1 UNKNOWN。(另:该批 run5/run9 BLOCKED 待查是 R3-H 复现还是 R3-I。)

**已知未解**(勿当已解决):OPEN-1 未收口(干净环境两批 16run 起飞15/16、通过13/16;R3-H降落门已修验证;残余=R3-I XY对齐帧一致性[B21/B22]+R3-G arm间歇[OPEN-1类,判别器UNKNOWN]);
A/A 扰动结论没有(ON 臂从未跑);真实 telemetry evidence 未验证;默认 10/10 未过;WP306 未开工。
本会话 mavlink-router fail-fast 加固=自造回归已回退(未测就上,0/10)。

**BIN-EKF+rosbag 分析已做(2026-07-22)**:候选推进到直接证据级——FCU/EKF 侧早期健康(输入66.9Hz/EKF3.3s收敛/
origin早设,CONTRADICTED 输入慢/EKF慢/origin慢);ROS 侧 external nav 全程新鲜(RTF=0.30,"40s硬停"是时钟误读=run末);
**直接观测 sender `ready` 每1-2s翻转而 odom 恒新鲜1ms→翻转项 local_position_fresh(推断)→FCU LP 输出0.9Hz(511限速)
→readiness 零迟滞门狂闪→攒不够5s→timeout**。判读=AA003_BIN_EKF分析_2026-07-22.md。(诊断历程记录;机制层候选链已定位,但 OPEN-1 未收口,见顶部⚠️。)

**P01+P02 已做(2026-07-26,经自检降级)**:aa-r1 rosbag 255帧+代码——**VERIFIED**:odom_fresh 255/255→外部导航输入排除;
readiness 失败全由 local_position not-fresh(**FCU LP 迟起 msgid32 首帧 sim-boot 5.5s/wall+21s 的 24帧 + 迟后仍慢 4.3Hz/12gap 的 20帧**);
external_nav.py:590 墙钟阈值+runtime_state.py:379 零迟滞。**级别 STRONG_SUPPORT 非 DIRECT-已定位**("44/44"是代码同义反复,自检已收回)。
最上游"FCU LP 为何迟+慢(EKF延迟/511/RTF 均候选未验)"UNKNOWN。判读=AA003_BIN_EKF分析 §8.1。
**下一停点**:**推荐单变量验证 A(提 FCU LOCAL_POSITION 输出率消 >1000ms gap),OFF-only,须负责人批**——改→红→绿→OFF-only≥3
(成败均入分母;存 tlog msgid32 gap+sender status+mission summary);支持=A 后 flap 消失且起飞,推翻=gap 清零但仍 flap。
B(加迟滞)/C(阈值改 sim 域)备选。是否重跑 AA003 取 ON 臂由负责人裁决。可并行:WP306、P2-OFFLINE-PREP。
**诚实红线**:A/A 无结论;OPEN-1 机制层已定位但**未收口**(sim-clock 候选 6c28d26 未验证,须默认 10/10,见顶部⚠️,不写已修复/已定位关闭);非稳定/非 10-10/非长稳/非 Review3 完成。

**更新时间**:2026-07-28(起飞不稳分解为QGC环境[已修]/R3-H降落门[已修验证]/R3-G-arm[OPEN-1类]/R3-I-XY对齐[B21/B22];OPEN-1保持OPEN,9/10≠10/10)

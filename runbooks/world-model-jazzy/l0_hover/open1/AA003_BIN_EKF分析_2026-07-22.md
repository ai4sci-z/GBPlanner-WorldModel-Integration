# AA003 aa-r1 BIN · FCU 内部 EKF/VISO/ORGN 时序分析(2026-07-22;树状令 P03)

> ⛔ **★唯一现行结论 = §6.1(2026-07-22 自纠正后)。必读,勿走旧分支。**
> 本文 §1-§4 中"external-nav 输入在 t≈40s **硬停**=决定性上游事件/输入中途断供"是**纠正前的推理,已被 §6 CONTRADICTED**
> (sim/wall 时钟误读:RTF=0.30,"sim-40.3s"=run 结束点非中途;rosbag 实测 ROS 侧 `/external_nav/odom` **全程新鲜** odom_age≈1ms
> 到 127.7s)。**§1-§4 的 cessation 结论一律 SUPERSEDED,仅保留为推理过程可审计,禁作为现行结论/施工依据。**
> **现行结论 = §8.1(§7 的"DIRECT/44-44"措辞已被 §8 自检降级,以 §8.1 为准)**:
> ①**VERIFIED**:odom_fresh 255/255 恒 true → 外部导航输入侧**排除**;readiness 失败全由 `local_position` not-fresh 解释,
> 分两半——**FCU LOCAL_POSITION 迟起(msgid32 首帧 sim-boot 5.5s/wall+21s,relay 11.9s)24 帧 + 迟起后仍慢/gappy(4.3Hz/12次>1000ms)20 帧**;companion 墙钟 1000ms 阈值(external_nav.py:590)+零迟滞门(runtime_state.py:379)→ 迟+慢 LP 致 readiness 反复清零→timeout。
> ②**级别=STRONG_SUPPORT,非"DIRECT 已定位"**("ready==fcu_local_position_ready 44/44"是代码同义反复,不作独立证据)。
> ③**§9 钻底(2026-07-26)**:tlog 直测——companion 从 6.2s 就请求 LP@20Hz(99次),但 FCU 只交付 0Hz→0.9Hz→5.5Hz;**"511迟/511爬升"两假设 CONTRADICTED**;稳态 5.5Hz≈20Hz×RTF0.30=**RTF 节流坐实**,前50s更慢=杀 readiness 的窗。**推荐单变量从 A 改 C(新鲜度阈值改 sim/RTF 域)**——A 无用(FCU 已被请求20Hz却不理)。级别 STRONG_SUPPORT(定位到域错配);UNKNOWN=前50s为何比RTF折算还慢(FCU/SITL早期出流延迟)。**不写"OPEN-1已定位"。** 见 §9/§9.1。

> 性质:冻结证据(EVIDENCE)。状态权威=CURRENT_STATUS.md。
> 输入=AA003 aa-r1 OFF 失败样本的 dataflash BIN(3.15MB,LOG_DISARMED=1 交付)。
> 解码=pymavlink DFReader_binary 2.4.49(权威;只读加载自 uv cache,未改任何生产/仓)。
> **上层已独立复验关键序列(时间基差~1.9s,序列一致)。结论按 STRONG_SUPPORT/CONTRADICTED/UNKNOWN 分列;不写"根因已定位"。**

## 0. 样本身份(P01 冻结)
- run=`world-model/artifacts/sim/hover/20260722T074436.360696048Z`;attempt=`AA003-20260722/attempts/aa-r1_OFF`
- tlog sha256 `61b75f2c…`(1,194,530B);**BIN sha256 `189eb68d…`(3,153,920B=3.2MB,65656×0xA395,可读OK)**
- mission_summary `10f04b3f…`;runtime parm `a6af6d01…`(LOG_DISARMED 1);approval `6078d955…`
- main@48cc7f6 / wm@6d412a11f152 / image sha256:c137d509 / frozen_plan_sha 3c41d8e…
- ⚠️ **BIN 只覆盖前 ~62s**(FCU boot 1.934→64.04s);mission ~224s,**失败尾段不在 BIN**。

## 1. 统一时间线(t_rel=相对 FCU boot;原始数字)
| t(s) | 事件 |
|---:|---|
| 0.01 | **external-nav 输入(VISP/VISV)首帧,~66.9Hz,Q=100/Rst=0/Ign=0** |
| 1.42 | ORGN Type0 EKF origin set(-35.363262,149.165237,584) |
| 2.00 | AHRS EKF3 active;EV25 SET_HOME;ORGN Type1 |
| 3.10 | XKF4 SS=65536(INITIALIZED) |
| 3.24 | 'IMU is using external nav data';tilt align complete |
| 3.27 | EV62 EKF_YAW_RESET;yaw aligned |
| **3.30** | **XKF4 SS=74623=att+hvel+vvel+hpos_rel+hpos_abs+vpos+using_gps+init(完整外部导航位置解)** |
| **38.39** | external-nav(VISP/VISV)BIN 最后一帧(abs boot 40.32s)。⚠️**此=BIN/run 结束点(RTF=0.30,sim-40s=run 末),非中途硬停;ROS 侧全程新鲜,见 §6.1**。旧读"硬停"SUPERSEDED |
| **40.0** | **'PreArm: VisOdom: not healthy'(输入停后~1.6s)** |
| 42.5 | XKF4 SS 74623→66431(using_gps bit 掉) |
| 48.5 | XKF4 SS 66431→65903(horiz_pos_abs 掉→仅相对位置解) |
| 62.1 | BIN 结束(XKF* 仍 ~19.7Hz;ARM 从未记录;MODE 恒 STABILIZE 未切 GUIDED) |

未记录(明确非零值冒充):GPS/NKF0-5/VISO/ARM=0 条;只跑 EKF3(无 EKF2)。

## 2. P03.2 九问逐条
1. **external nav 是否进入 FCU**:**是**。VISP/VISV 从 t=0.01 稳定 66.9Hz 进 FCU,EKF 明确"using external nav data"(t=3.24)。
2. **VISO/VISP 是否稳定**:**是,Q=100/Rst=0/Ign=0/零 gap>1s**,直到 BIN 末帧 sim-40.3s。⚠️**"之后硬停 23.7s"= SUPERSEDED**(sim-40s=run 结束点,rosbag 证 ROS 侧全程新鲜,见 §6.1)。VISO(odometry)从未记录,只有 VISP(position)+VISV(velocity)。
3. **ORGN 是否按预期建立**:**是**,t=1.42 设 EKF origin、t=2.0 设 home,早且干净。
4. **EKF 是否拒/延迟/反复 reset**:**早期否**——3.3s 即达完整解,无拒无反复 reset。BIN 末段(run 结束/teardown 附近)EKF 降级(SS 74623→66431→65903,掉 using_gps 再掉 hpos_abs)——这是 **run 末外部导航停供的后果,非 EKF 自身故障,亦非中途硬停(§6.1)**。
5. **LP 慢启动是输入慢/EKF融合慢/origin慢/输出发布慢**:输入/EKF/origin **全不慢**(见上)。tlog 那个 LP(msgid32) 0.9Hz 是 **FCU 输出发布速率**(511 SET_MESSAGE_INTERVAL 流爬升)=**输出发布慢**,与 66.9Hz 的输入是两个不同信号。⚠️~~决定性失败是 t≈40s 输入硬停~~ **SUPERSEDED**:见 §6.1——**决定性是输出发布慢经 local_position 反馈致 readiness 狂闪**。
6. **60s 内为何攒不够连续 5s readiness**:⚠️~~external-nav 输入 40s 硬停→...~~ **SUPERSEDED(§6.1)**。现行:FCU LP **输出**约 0.9Hz(>1000ms gap)→sender `local_position_fresh`(推断)闪断→`ready`+mission readiness 门每1-2s翻转→零迟滞门(runtime_state.py:368-379 单样本清零5秒)攒不足连续5s→preflight_timeout。external nav 输入全程新鲜(§6),EKF 早期健康。
7. **与 AA001-r3 是否同链**:**同表型**(preflight_timeout/never arm),AA001-r3 的"LP 慢启动"(tlog)= 本样本的 LP **输出** 511 爬升,一致且互证。~~"输入硬停机制"~~ SUPERSEDED(无中断)。aa-r3 是否同经 readiness 狂闪=**UNKNOWN**(无 sender status/BIN)。故:同表型、机制推进、不能断言同一条链。
8. **哪些竞争假设被削弱**:
   - **CONTRADICTED**:"EKF 融合慢/慢启动"(3.3s 达完整解);"external-nav 输入慢启动/中途硬停"(t=0.01 起 66.9Hz,ROS 侧全程新鲜);"origin 慢"(1.4s 设)。
   - **STRONG_SUPPORT(纠正:非 WEAKENED)**:**LP 输出慢(511 爬升)经 local_position 反馈致 readiness 狂闪 = 驱动**(直接观测 ready 翻转,§6.1),非次要。
9. **哪些仍 UNKNOWN**:①`local_position_fresh` 为翻转项属**推断**(sender status 未直接登记该字段,P01 待离线对齐 LP gap 时刻证实);②FCU LP **输出** 511 速率为何这么慢(爬升上游);③aa-r3/历史 no-BIN 是否同经此 readiness 狂闪;④XKFS/XKFM 部分字段未逐位核(标 UNKNOWN 未猜)。

## 3. ~~P03.3 结论~~ ⛔ SUPERSEDED(全节被 §6.1 取代;2026-07-22)
> **本节旧结论"external-nav 输入 40s 硬停=决定性上游事件"= sim/wall 时钟误读,已被 §6.1 CONTRADICTED。现行结论见 §6.1。以下划线保留仅为审计,禁作为现行结论。**
- ~~STRONG_SUPPORT:external-nav 输入流在 t≈40s 硬停=决定性上游事件~~ → **SUPERSEDED**:非硬停,是 run 结束点;ROS 侧全程新鲜(§6)。
- **CONTRADICTED(此条仍成立)**:FCU/EKF 侧慢(输入慢/EKF 融合慢/origin 慢)三个子假设——本样本 FCU 侧早期完全健康。
- ~~"为何 40s 停"、"空窗 23.7s"、"选择器过渡态停发"~~ → **SUPERSEDED**:基于误读的 cessation,不成立(无中断)。现行 UNKNOWN 见 §6.1。

## 4. ~~P04 旧下一步(companion 停发排查)~~ ⛔ SUPERSEDED(被 §6.2 取代;2026-07-22)
> **本节旧下一步"查 companion 为何在 40s 停发"基于误读的 cessation,不成立。现行单变量验证方案见 §6.2(核 local_position_fresh/LP gap 翻转对齐)。**

## 6. ★rosbag 决定性发现(2026-07-22 追加;纠正 §1-3 的"中断"解读并推进到直接证据)
读同 run 的 rosbag(hover_rosbag_0.mcap,zstd CLI 解压 + mcap lib,只读):
- **时钟域实测**:`/external_nav/odom` header.stamp(sim) 1.88→40.08s 而 wall 0→127.7s → **RTF=0.30**。故 BIN 的
  "VISP 停在 sim-40.3s"**= run 结束点**(run 只到 sim-40s 因 RTF 跑了 127.7s 墙钟),**不是中途断供**。→ **§2-Q2/§3
  的"external-nav 40s 硬停"是 sim/wall 时钟误读,纠正**:external nav ROS 侧**全程流且新鲜**(odom_age≈1ms 到 127.7s)。
- **/slam/odom、/external_nav/odom、/odom_candidate n≈26668 均流到 127.7s**;selector status blockers=
  `["not_hover_correction_phase"]`(=从未进 hover 阶段,因从未 arm),last_accepted_age≈3ms(新鲜),degraded=false。
- **★直接观测到 readiness 狂闪**:`/mavlink_external_nav/status`(sender 自报)全程 state=sending、**odom_fresh=true
  (age≈1ms)**、sent_count 递增,但 **`ready` 每 ~1-2s 在 true/false 间翻转**(wall+12.4 true→13.9 false→14.4 true→
  15.9 false…全程如此)。按 external_nav.py:597 `ready=sending∧sent_count>0∧odom_fresh∧local_position_fresh`,
  odom 侧全满足→**翻转项=`local_position_fresh`**(FCU 的 LP 输出反馈新鲜度;**推断**,status 未直接登记该字段)。
  而 tlog 显示 FCU 的 LOCAL_POSITION_NED **输出** 0.9Hz(511 速率爬升,>1000ms gap)。

### 6.1 结论升级(仍守纪律,未写"已定位")
- **CONTRADICTED(纠正自身)**:"external-nav 输入中途硬停"——sim/wall 误读;ROS 侧全程新鲜。
- **STRONG_SUPPORT(直接证据,较 §3 更进一步)**:**FCU 的 LP 输出流慢(511 限速 × RTF=0.30 墙钟)→ companion 的
  local_position 新鲜度闪断 → sender `ready` + mission readiness 门狂闪(直接观测:ready 每1-2s翻转而 odom 恒新鲜1ms)
  → 零迟滞门(runtime_state.py:368-379 单样本清零5秒)永攒不够连续5s → preflight_timeout → never arm**。此链与
  AA001-r3(tlog LP 0.9Hz)、Q2 §12 时钟域机制(墙钟阈值 vs sim 发射,RTF 收紧)完全自洽,且**首次直接观测到 ready 翻转**。
- **仍 UNKNOWN(未满足"已定位"全条)**:①`local_position_fresh` 是翻转项为**推断**(status 未直接登记,须核 sender/mission
  对 /navlab/fcu/local_position_pose 的新鲜度判定);②FCU LP 输出 511 速率为何这么慢(爬升曲线上游);③是否所有失败
  样本同此(仅 aa-r1 直接证)。故仍不写"OPEN-1 已定位",但候选**从"external nav readiness 慢启动"推进到"FCU LP 输出
  反馈慢→local_position 新鲜度闪→readiness 门闪"的直接证据级**。

### 6.2 P04 修正后的单变量验证方案(不直接改,待负责人批)
- **[离线可先做]** 核 sender/mission 代码:确认 `local_position_fresh`/`fcu_local_position_ready` 的新鲜度阈值(1000ms 墙钟?)
  与 FCU LP 输出率的关系;核 tlog 的 msgid32(LP) 逐帧 gap 与 sender ready 翻转时刻是否对齐(可离线证实翻转因)。
- **[单变量修复候选,须批]** 三选一验证:①提高 FCU LP 输出率(SET_MESSAGE_INTERVAL 让 LP 更快,消 >1000ms gap);
  ②readiness/sender ready 加迟滞(别单帧翻转清零);③新鲜度阈值改 sim-time 域(按 RTF 折算,§12)。每个都是单变量,
  改→红→绿→OFF-only 重跑验(成功/失败均入分母);目的=验证"消除 LP 输出反馈闪断能否让 readiness 攒够 5s"。

## 5. 工具能力边界(诚实)
pymavlink DFReader(权威)已用,3 反例过(缺 FMT 不解析/0 条不冒充零值/停顿不当连续健康)。**不能从本 BIN 证**:external-nav 为何停(companion/DDS 侧);224s 尾段(BIN 只 62s);aa-r3 同机制(无 BIN)。XKFS/XKFM 部分字段标 UNKNOWN 未猜。跨视角(tlog 输出 0.9Hz vs BIN 输入 66.9Hz)已在 §2-5 释清=两个不同信号,非矛盾。

## 7. ★P01-P03 收口:readiness 翻转项 DIRECT 定位 + 单变量方案(2026-07-26)
### 7.1 P01 DIRECT(逐帧对齐,非推断)
从 aa-r1 rosbag 抽 `/mavlink_external_nav/status` 全 255 帧(sender 直接登记 `fcu_local_position_ready`/`local_position_age_ms`/`local_position_count`):
- **`ready=false` 44 帧,其中 `fcu_local_position_ready=false` = 44/44(100%)**;`odom_fresh=true` = 44/44(100%)。→ **翻转项 = local_position 新鲜度,DIRECT(非推断);外部导航输入侧 100% 无责**。
- `local_position_count` 0→494(收到了,但迟:首条 @11.9s);`local_position_age_ms` 中位 50ms 但 **max 3350ms、20 帧 >1000ms、早期 24 帧=-1(未收到)**。
- `/navlab/fcu/local_position_pose`(FCU LP 输出→ROS)实测 **4.30Hz、max gap 3400ms、12 次 >1000ms、首帧迟到 11.9s**。
- 证据:`scratchpad/open1_bin/p01_align.csv`。**P01.4 等级 = DIRECT。**

### 7.2 P02 代码映射(只读)
- `external_nav.py:593-597`:`local_position_fresh = local_position_count>0 ∧ 0≤local_position_age_ms≤max_local_position_age_ms(1000)`;`ready=sending ∧ sent_count>0 ∧ odom_fresh ∧ local_position_fresh`;age 用 **`time.monotonic()` 墙钟**(:588-590);status `fcu_local_position_ready`=`local_position_fresh`(:633)。
- `runtime_state.py:346`:mission `fcu_local_position_ready = fcu_local_position_ready ∧ mavlink_external_nav_fresh`;:368-372 进 5 布尔 `ready_for_preflight`;**:375-379 单样本零迟滞:任一 not-ready→`ready_started=None`→连续5秒倒计时归零**;freshness 用墙钟(:333/337/341,`status_timeout_sec`)。
- **P02.3 时钟域判定 = STRONG_SUPPORT**:新鲜度阈值(墙钟 1000ms)vs FCU LP 输出率(受 SET_MESSAGE_INTERVAL/511 sim-time × RTF=0.30 节流)→ RTF<1 时有效阈值收紧(§12 算术:墙钟 1000ms ⇔ 仿真钟 300ms ⇔ 需 ≥3.33Hz sim)。

### 7.3 完整链(DIRECT + 代码确认;仍不写"OPEN-1 已定位"因 511-慢的**最上游**因未定)
FCU LOCAL_POSITION 输出慢/稀(4.3Hz/12 次>1000ms gap/迟到 11.9s;511×RTF=0.30)→ sender `local_position_fresh` 墙钟龄破 1000ms 闪断(DIRECT)→ sender `ready` 闪(44/44)+ mission `fcu_local_position_ready` 闪 → 零迟滞 5 布尔门倒计时反复归零 → 攒不足连续 5s → 60s 预算耗尽 → preflight_timeout → never arm。**机制层已 DIRECT 定位;仅剩最上游 UNKNOWN:FCU LP 511 输出为何这么慢/迟到(ArduPilot SET_MESSAGE_INTERVAL 协商侧)。**

### 7.4 P03 单变量验证方案(定稿,不直接改;OFF-only,须负责人批)
| 候选 | 目标 | 风险 | OFF-only 验收 |
|---|---|---|---|
| **A(推荐)提高 FCU LOCAL_POSITION 输出率** | 消 >1000ms gap(SET_MESSAGE_INTERVAL 让 msgid32 更快/更早) | 改 MAVLink 输出负载,轻微动比较基线 | LP gap>1000ms 清零或骤降 + `ready`/readiness 门不再闪 + 攒足连续5s + 起飞成功 → **直接证实本链** |
| B readiness 加迟滞 | 防单帧 not-fresh 清零 5s 窗 | 可能掩盖真实 external nav 中断 | 须同时证 odom_fresh 与 LP gap 在安全阈内,不靠忽略坏数据过门 |
| C 新鲜度阈值改 sim 域(RTF 折算) | 消墙钟阈值×sim 输出频率错配 | RTF 变化时阈值动态复杂 | 同一失败样本重放由 flapping 转稳定 + OFF-only 通过 |
**推荐 A**:①最直接证伪/证实当前 DIRECT 链(慢→闪的因果)②单变量③易回滚④不掩盖真实故障。C 为可能的生产终态修法。
**OFF-only 验证设计**(不申请 A/A、不开 telemetry):样本 OFF-only ≥3;成功/失败均入分母;须存 tlog(msgid32 gap)+ sender status(ready/fcu_local_position_ready)+ mission summary;**停止条件**=拿到"A 生效后 LP gap 清零且 readiness 攒足 5s 起飞成功"或"改后仍 flapping"任一即停;**支持候选**=A 后 flap 消失且起飞;**推翻候选**=A 后 LP gap 已清零但 readiness 仍 flap(则翻转另有因)。

## 8. ⚠️自检纠正(2026-07-26;负责人指"赶工",复核后降四处虚高)
**本节纠正 §7 的过度声称。§7 的 DIRECT 措辞降级,以本节为准。**
1. **"ready=false ⟺ fcu_local_position_ready=false 44/44=100% DIRECT 相关"= 同义反复,非独立证据(降级)**:实测 `odom_fresh` 全程 **255/255 恒 true**;而 `ready=sending∧sent_count>0∧odom_fresh∧local_position_fresh`(external_nav.py:597)、status `fcu_local_position_ready`=`local_position_fresh`(:633)。odom_fresh 恒 true + state=sending ⇒ `ready≡fcu_local_position_ready` 是**代码定义**,44/44 只是复述定义,不是"发现的相关性"。**真正有效的证据是**:①odom_fresh 255/255 → 外部导航输入侧 100% 排除(此条成立);②readiness-false 全由 local_position not-fresh 解释。
2. **44 帧漏报一半机制(补全)**:ready=false 44 = **age=-1(LP 流早期尚未起来)24** + age>1000ms(收到但陈旧)20。**"迟到/未起"(24/44)是主体,不只是"慢/gap"(20)**。§6.1/§7 只说"慢"是不完整。
3. **"FCU LP 迟到"此前未证,现补测坐实**:tlog msgid32(FCU 侧 LOCAL_POSITION 输出)**首帧 sim-boot 5.55s / tlog+21.4s wall**(tlog 首记录 1784706281.86→msgid32 首帧 1784706303.27),与 relay /navlab/fcu/local_position_pose 首帧 11.9s(rosbag)对齐 → **FCU 输出本身迟+早期 0.9Hz 稀,非仅 relay 迟**。此条现有测量支持(此前是未证断言,已补)。
4. **"511×RTF 是机制"降为假设(未验)**:本轮只测到"FCU LP 输出迟(5.5s sim起)+ 慢(4.3Hz/wall早期0.9Hz/12次>1000ms gap)"。"因 SET_MESSAGE_INTERVAL(511)协商 × RTF=0.30 致慢"是 AA001-r3 的**候选假设**,本轮**未独立验 511 interval**,§7 当既定机制写属过度。

### 8.1 纠正后的诚实结论级
- **VERIFIED(本轮直接测)**:外部导航输入清白(odom_fresh 255/255);readiness 失败全由 local_position not-fresh 解释,分两半——**FCU LP 输出迟起(~sim 5.5s / wall 21s)24 帧 + 迟起后仍慢/gappy(4.3Hz,12次>1000ms)20 帧**;companion 用墙钟 1000ms 阈值(external_nav.py:590)+ 零迟滞门(runtime_state.py:379)→ 迟+慢的 LP 致 readiness 反复清零 → timeout。
- **级别 = STRONG_SUPPORT(非"DIRECT 已定位机制")**:链条自洽且各环有测量,但"44/44"不作独立证据、且最上游未定。
- **UNKNOWN(未验)**:FCU LP 为何迟起+慢(EKF-解到首次输出的延迟?511 interval 协商?RTF 交互?——均候选,未证);aa-r3/历史样本是否同此。
- **推荐单变量 A 不变但目的收窄**:验证"消除 LP 迟起+gap 能否让 readiness 攒足 5s";若 A(提率)只解决"慢"不解决"迟起",可能仍失败→那将指向 EKF-到-输出延迟而非纯速率,是有用的证伪。

## 9. 最上游钻底(2026-07-26):511 假设推翻,定位到 RTF 节流×墙钟阈值;推荐从 A 改 C
**VERIFIED(tlog pymavlink 直测,aa-r1):**
- **companion 从 @6.2s 起请求 `SET_MESSAGE_INTERVAL(511)` msgid32=LOCAL_POSITION_NED@20Hz(50000us)**,共 99 次每 2s 重复到 136s。→ **推翻两个候选假设**:①"511 请求迟"(实际很早 6.2s)②"511 速率爬升/协商慢"(一直请求 20Hz,不爬升)。
- **FCU 实际交付率**:[0-20s]**0Hz**(不出)→[20-50s]**0.9Hz**→[50-150s]**~5.5Hz**→尾段降。首帧 @21.4s wall。
- **稳态 5.5Hz ≈ 20Hz×RTF(0.30)**:坐实 FCU 按 **sim 钟**排 20Hz,SITL RTF=0.30 → 墙钟实交付 ~6Hz。**域错配坐实**:FCU 输出 sim 排程+RTF 节流,companion `local_position_age` 用**墙钟** 1000ms 阈值(external_nav.py:590)。
- **早期 0-0.9Hz(前 50s)比 RTF 折算(~6Hz)还低**=真正杀 readiness 的窗:起飞 60s 预算(墙钟)几乎全落在 LP 还没起来/极慢的前 50s → 零迟滞门永攒不足 5s → timeout。

**结论级别(仍守纪律)**:
- **STRONG_SUPPORT(定位到域错配)**:失败=FCU LP 输出(sim 排程+RTF 节流,且早期更慢/迟起 21s)与 companion 墙钟 1000ms 阈值+60s 预算不兼容。**"511 是机制"CONTRADICTED**(§8.1-4 的假设本节证伪)。
- **UNKNOWN(未完全隔离)**:前 50s 为何比 RTF 折算(~6Hz)还低到 0-0.9Hz(EKF 收敛后到首输出的 FCU 内部延迟?SITL 调度器早期优先级?)——已缩到"FCU/SITL 早期出流延迟"这一点,但未逐位隔离。

### 9.1 ★推荐单变量从 A 改 C(据本节证据纠正 §7.4/§8.1)
- ~~A 提 FCU LP 输出率~~ **降级**:FCU 已被请求 20Hz 却只出 0.9-5.5Hz(RTF 节流+早期慢),提"请求率"无用(请求已 20Hz);除非改 SITL/ArduPilot 侧出流,超出 companion 可控且动基线。
- **C(推荐)新鲜度阈值改 sim-time/RTF 折算**:companion `local_position_age`/`max_local_position_age_ms` 从墙钟改按 sim 钟(或按 RTF 放宽,§12 算术:墙钟 1000ms→需覆盖 RTF=0.3 下 sim 排程)。**最直接对准域错配根**、单变量、不改 FCU/不动 SITL 基线、易回滚。验收=同失败样本重放由 flapping 转稳定 + OFF-only 起飞成功。
- B 加迟滞:次选,治标(防单帧清零)但不解域错配,且可能掩盖真实中断。
- **OFF-only 验证不变**:改 C→红→绿→OFF-only≥3(成败入分母;存 tlog msgid32 率+sender status+summary);支持=C 后 readiness 攒足 5s 起飞,推翻=C 后仍 flap(则另有因)。

## 10. ★单变量干预验证结果(2026-07-26;真实 OFF-only ×3)
**实施**:wm@`23116ca`(fix 分支,已推 backup)——唯一变量 `--max-local-position-age-ms 1000→4000`
(runtime_specs.go:507+测试,先红后绿,tasks 包全绿;依据=aa-r1 实测 LP max gap 3.4s+18% 余量)。
**验证**:OFF-only ×3(l2_batch,BC_ROOT=/home/ai4s/aa_runs/OPEN1-FIXC-20260726,wm_head=23116ca 记录在 batch log,
4000 已核实进活容器 Args/runtime_plan/service_handles):
| run | rc | armed | airborne | 终态 | 墙钟 |
|---|---|---|---|---|---|
| 20260726T082851 | 0 | ✓ | ✓ | S13 task_success | 3m28s |
| 20260726T083229 | 0 | ✓ | ✓ | S13 task_success | 3m16s |
| 20260726T083555 | 0 | ✓ | ✓ | S13 task_success | 3m39s |
**分母=3/3/3(发起/起飞/full-pass),无失败无剔除。**
**机制层核验(run1 rosbag)**:ready=false 仅 22 帧且**全部在 0-10.5s**(FCU LP 迟起窗,预算内无害);
**11.0s 首次 ready=true 后全程翻转=0**(修复前 aa-r1:全程每 1-2s 狂闪 44 帧);
local_position_age 中位 300ms、**max 3800ms<4000 阈值、超阈 0**。
**结论(严格措辞)**:
- **候选链获干预级验证(最强证据类)**:单变量(阈值 1000→4000)介入→预测的"闪断消失+门攒足5s+起飞"全兑现,3/3。
  链="FCU LP 输出 RTF 节流+迟起 × companion 墙钟 1000ms 阈值 × 零迟滞 5s 门"成立。
- **⚠️薄余量如实记**:max age 3800 vs 阈值 4000 仅 200ms 余量——更差 RTF/负载下可能再破。**4000 是止血,
  耐久修法仍是阈值改 sim-time/RTF 域**(登记下一步,非本轮)。
- **禁写**:OPEN-1 已关闭/稳定/10-10/长稳——**3/3≠10/10**,OPEN-1 保持 OPEN 待 WP307 默认 10/10(应基于
  wm@23116ca 基线,待负责人裁决 FUTURE_CANDIDATE 前移)。

# AA001-r3 定向分析(2026-07-21;只读离线;零仿真启动)

> 性质:冻结证据(EVIDENCE)。状态权威=CURRENT_STATUS.md。
> 目标=榨干 AA001 aa-r3_OFF 的 `waiting_for_fcu_external_nav` 失败对 OPEN-1 的定位价值。
> **blocker 标记≠根因**;本文全部结论为**候选链**,按支持/反对/UNKNOWN 分列。

## 1. 证据锁定

| 项 | 值 |
|---|---|
| AA001 根 | `/home/ai4s/aa_runs/AA001-20260721` |
| main / wm | `3f5f40729d62…` / `9a1ce95c56e2…` |
| 镜像 digest | `sha256:2aab049e96f0…` |
| config / runtime plan hash | `77079f79…63e9` / `b8c195ad…03d5`(frozen_plan_sha=dc700eef…) |
| aa-r1_OFF ✅ | wm run `20260721T133202.201636611Z`;13:32:01→13:35:19Z rc=0;BIN 在 |
| aa-r3_OFF ❌ | wm run `20260721T133519.694676932Z`;13:35:19→13:39:07Z rc=1;**无 BIN**;tlog 1,172,622B 完整可解析(14,638 帧) |

## 2. 同口径对照(全部只读:extractor/tlog 帧级/mission_summary/wm 冻结源码)

| 观察量 | SUCCESS aa-r1 | FAIL aa-r3 | 判别? |
|---|---|---|---|
| companion→FCU ODOMETRY(331) | 2040 帧,20Hz 稳定 | 2700 帧,20Hz 稳定(sent 821,odom_age 3ms,state=sending) | ✗ 两侧健康 |
| ROS 侧 external_nav_ready | True | True(/slam/odom 229Hz,slam_quality good) | ✗ |
| **FCU→companion LOCAL_POSITION_NED(32),前 40s** | 20-40s 桶≈5Hz(101/20s) | **0.8-2.6Hz(16、52/20s)** | **✅ 主判别** |
| 同上,60s 后 | ~5Hz | **也收敛到 ~5Hz(104-118/20s)**——但 mission 已 abort | 定位到"启动爬升期" |
| fcu_local_position_ready 抖动 | 0/40 采样 False | **12/40 False**(age 反复破 1000ms 阈值) | ✅ |
| ekf_flags_seen | [895,1024] | [895,1024] | ✗ 相同 |
| gps_global_origin / home_position | True/True | True/True | ✗ |
| SET_MESSAGE_INTERVAL(511) ack | 32×accepted+**4×DENIED** | 52×accepted+**8×DENIED** | ✗ 两 run 均有(非判别器;FAIL 请求总数更多=桥重试症状) |
| ARM(400) | 4×accepted,armed/airborne True | **从未发出**(mission 未过 ready 门) | 后果非原因 |
| BIN | 在 | **无**(LOG_DISARMED=0,docs/ori.param L490) | 后果:未 arm→不落盘 |

权威判定语义(wm 冻结源码 `navlab/common/companion/mission/stages/hover.py`):
`waiting_for_fcu_external_nav` = `not mavlink_external_nav_ready or not fcu_local_position_ready`
(L181-184);ready 需 age<1000ms 且**连续稳定 preflight_ready_sec=5.0s**(L189,L70);
总等待预算 `max_wait_sec=60.0`(L61)。FAIL 的 history(elapsed 15.5-35s)中最长连续
ready 段≈3s,从未攒足 5s。

## 3. 最强候选链(候选,非 CONFIRMED)

> **SITL FCU 的 LOCAL_POSITION_NED 输出存在随机时长的慢启动爬升期 → 当爬升慢于
> 新鲜度阈值(1000ms)时 `fcu_local_position_ready` 持续抖动、无法攒足连续 5s 稳定 →
> 60s 等待预算耗尽 → `hover_mission_abort:waiting_for_fcu_external_nav` → 从未 arm →
> (LOG_DISARMED=0)无 BIN。**

一条链同时候选解释三件事:no-BIN 类失败(=从未 arm,非"死得早")、间歇性(爬升时长
随机,非代码差异)、同 commit 成败并存(时序变异)。

- **支持**:①两向桥证据健康,排除 companion→FCU 方向;②帧级全程速率判别(前 40s:
  0.8-2.6 vs 1.75-5Hz),且 FAIL 在 60s 后也收敛=启动期现象而非永久故障;③预算权威值
  与 history 抖动模式自洽;④EKF flags/origin/home 两 run 相同,无 EKF 拒绝证据;
  ⑤SUCCESS 同样存在爬升期只是更快——连续谱而非二元故障。
- **反对/削弱**:①样本 1v1;②511 DENIED 与总请求数差异方向一致但非独立证据;
  ③history 为 40 条滚动窗,SUCCESS 前 15s 细节缺失(帧级已补足此缺口)。
- **UNKNOWN**:①FCU 侧为何慢启动(EKF 内部时序?SITL 调度?interval 生效延迟?)——
  失败臂**无 BIN=FCU 内部盲区**,现有证据不足以再往下钻;②历史 no-BIN 失败
  (205113/210149,eab0cc6 基线)是否同链——可同法回放验证,按冻结纪律仅登记;
  ③ready 连续段精确长度(0.5s 采样粒度限制)。

## 4. 可证伪观察量(下一步)

1. **帧级预测(可立即离线证伪)**:若链成立,任何 no-BIN 失败 run 的 tlog 前 60s 内
   必无"连续 5s 的 LOCAL_POSITION_NED age<1000ms"段;任何成功 run 必有。可对历史
   五 run tlog 批量回放(登记,未执行)。
2. **AA002 必补观测字段**:失败臂 FCU 内部时序不可判读的根因=无 BIN。
   **建议 AA002 计划加 `LOG_DISARMED=1`**(参数级,进 canonical config→新 frozen
   plan+新审批;非代码改动)——失败臂将获得 EKF/XKF 内部时序,直接检验"慢启动"的
   上游原因。此字段缺失会让 AA002 的失败臂重蹈"不可判读"。
3. mission ready 采样全程保留(代码改动)→登记下一编号 Review,非必需(tlog 帧级可替代)。

## 5. 四问答案(令文要求)

1. AA001 是否产生 telemetry 扰动结论:**没有**(ON 臂未启动,OFF/ON 不可比较)。
2. AA001 是否产生 OPEN-1 新证据:**有**——no-BIN 类首次受控复现+完整候选链+
   "no-BIN=从未 arm"的统一候选解释+帧级判别方法。
3. aa-r3 失败最强候选链:见 §3。
4. AA002 建议:**调整观测后再跑**——LOG_DISARMED=1 进计划(其余原样:同 commit/
   镜像/域/预算);不改任何生产代码。

## 6. 机制细化(2026-07-21 追加;time_boot_ms 仿真钟 vs tlog 墙钟直接测量)

对 msgid 32 逐帧提取 payload `time_boot_ms`(仿真钟)与 tlog 落盘时戳(墙钟):

- **RTF 两 run 全程恒定 0.30**(每 20s 窗口无一偏离)——**排除"宿主负载导致 RTF 抖动"
  作为本对样本的判别变量**(注意:不排除历史 RTF≈0.08 时代该因素;见下)。
- 真正的判别变量=**FCU 仿真钟内的 LP 发射速率爬升曲线**:
  SUCCESS 首窗 7.5Hz(sim)→20s 后 15.8Hz;FAIL 首窗 **3.0Hz(sim)**→8.9→40-60s 才 19Hz。
  目标速率≈17-19Hz(sim),爬升由 SET_MESSAGE_INTERVAL(511) 生效过程决定(初期部分 DENIED)。
- **时钟域错配算术**:companion 的新鲜度阈值 age<1000ms 是**墙钟**;LP 按**仿真钟**
  发射。RTF=0.30 时,墙钟 1000ms ⇔ 仿真钟 300ms ⇔ 需要 **≥3.33Hz(sim)** 才能稳定达标。
  FAIL 首窗 3.0Hz(sim)=0.9Hz(墙)——恰好骑在阈值线上 → ready 抖动;SUCCESS 首窗
  7.5Hz(sim)=2.25Hz(墙) → 稳定达标。**判定门实际上不在测"EKF 位置健康",而在测
  "此刻仿真跑得够不够快×流爬升到了没有"。**
- 回溯推论(候选):历史 RTF≈0.08 时代,墙钟 1000ms ⇔ 仿真钟 80ms ⇔ 需 12.5Hz(sim)
  才达标——爬升期几乎必然不 ready,与历史 no-BIN 高发自洽;R003 规则 11(宿主负载
  诱发 bring-up 失败)获得具体机制:负载→RTF↓→有效阈值线性收紧。
- 候选链修订(最强,仍非 CONFIRMED):**511 流速爬升的随机性 ×(墙钟阈值/RTF)的
  有效收紧 → ready 抖动 → 60s/连续5s 预算耗尽 → abort → 未 arm → no-BIN。**
- 修复方向(仅登记,不实现,属 wm 生产改动待负责人裁决):①新鲜度阈值改仿真钟域
  (按 RTF 折算);②等待窗起点改为"511 确认目标速率生效之后";③AA002 的
  LOG_DISARMED=1 建议不变(验证 FCU 内部 511 处理时序)。

# AA003 结果 · OPEN-1 失败判读(2026-07-22;树状令 P04-F)

> 性质:冻结证据(EVIDENCE)。状态权威=CURRENT_STATUS.md。
> **AA003 未完成 · A/A 无结论**(仅 aa-r1 OFF 跑完即失败,aa-r3/aa-r2/aa-r4=NOT_STARTED,无 OFF/ON 对比)。
> 本文=P04-F 失败样本冻结 + OPEN-1 判读。全部结论按支持/反对/UNKNOWN 分列;blocker≠根因。

## 0. 执行事实(树状令 P02-P03)
- 负责人明确批准 exact SHA(2026-07-22"直接干"指令);approval 绑 frozen_plan_sha256=
  `3c41d8e88bdb0fcbee9b1c0b2044a9d865c0920ef9a43e96f6796c1abf453144`,机器自校验 ok/reasons=[]。
- P02.3 不可变复核全过(main 48cc7f6 净/wm 6d412a11 净/digest c137d509/SHA 一致)。
- P03 `--execute` 正式入口,approval 过门(producer_started=1,preflight READY,refusal=[])。
- **aa-r1 OFF 失败即停**:aa-r3/aa-r2/aa-r4 标 `NOT_STARTED_PRIOR_FAILURE`(入分母),未凑成功。

## 1. P04-F.1 失败样本冻结
| 项 | 值 |
|---|---|
| failed attempt | **aa-r1** |
| OFF/ON | **OFF** |
| producer rc | run_rc=1 / agg_rc=10;producer_outcome=FAILED |
| abort reason | **preflight_timeout**(mission_phase_state=`S1 wait_nav_ready`) |
| start/end | 07:44:36Z → 07:48:20Z(**~3m44s**=pre-arm 超时签名) |
| tlog | `<run>/sitl/mav.tlog`(1,194,530B,652 msgid32 帧,crc_failed=0) |
| **BIN** | **`<run>/sitl/logs/00000001.BIN`(3,153,920B)存在** ← LOG_DISARMED=1 生效 |
| mission_summary | `<run>/mission_summary.json`(armed_seen=false,airborne_seen 无,ok=false,reason=preflight_timeout) |
| runtime mav.parm | `<run>/runtime/config/gazebo-iris-rangefinder.parm`,`LOG_DISARMED 1`@行72 |
| image digest | `sha256:c137d509…` | main/wm | `48cc7f6` / `6d412a11f152` |
| telemetry | OFF(evidence_state OFF/process OFF/finalization OFF;evidence_status COMPLETE,cleanup CLEAN) |
| 容器残留 | 运行后 0(现场干净) |
> `<run>`=`world-model/artifacts/sim/hover/20260722T074436.360696048Z`

## 2. P04-F.2 失败链判读
| 问 | 答 | 依据 |
|---|---|---|
| 是否 arm | **否** | armed_seen=false,卡 S1 wait_nav_ready |
| **是否有 BIN** | **是** | 00000001.BIN 3.1MB;**LOG_DISARMED=1 令未 arm 也留 BIN** |
| 无 BIN 原因 | N/A(有 BIN) | ——AA001 的"no-BIN=FCU 内部黑盒"缺口**本轮消除** |
| 是否 waiting_for_fcu_external_nav | **是** | S1 wait_nav_ready + reason=preflight_timeout |
| LOCAL_POSITION_NED 慢启动 | **是** | 前20s 墙钟 **0.9Hz**(18帧)→3.1→5.65;前60s **12 次帧间隔>1000ms**;median 166ms/max 3355ms |
| external nav 进入 FCU | 有输入证据 | BIN 含 VISO/VISP/VISV(视觉里程计);深度融合时序待 BIN-EKF 分析 |
| readiness 连续达标 | **否** | 60s 预算内从未攒足连续 5s(preflight_timeout) |
| 与 AA001-r3 同链 | **是(强支持)** | 同表型:preflight_timeout+never arm+~3m44s+前窗墙钟慢(0.9 vs AA001-r3 FAIL 0.8-2.6Hz) |

**支持**:①mission 权威 reason=preflight_timeout+never arm;②帧级前窗墙钟 0.9Hz 落在 AA001-r3 FAIL 区间;
③12 次>1000ms 帧间隔=readiness 连续性打断源;④与 AA001-r3 表型一致。
**反对/削弱(诚实)**:①样本仍 1(仅 aa-r1 OFF);②`has_ge5s=Y`(最长连续<1000ms 段 31s)——**LP 帧连续性
≠ readiness AND-门连续性**(readiness 是 5 布尔合取×单样本零迟滞 reset,早期稀疏帧+任一布尔闪断即清零,
与"某处存在长连续段"不矛盾),故 has_ge5s 不证伪链但也不能当直接支持;③ON 臂未跑=无 A/A 对比;
④sim 首窗 9.65Hz(**高于 AA001-r3 记的 FAIL 3.0Hz**)——**微差,说明 sim 速率本身非干净判别,墙钟率
(sim×RTF=0.9Hz)才是**,反而印证 §时钟域错配机制(墙钟阈值 vs 仿真钟发射)。
**UNKNOWN(本轮新可查)**:FCU 内部**为何**慢启动(EKF 拒/延迟融合视觉?origin 时序?511 interval 生效延迟?)
——**AA001 全黑,AA003 的 BIN 现含 XKF0-5/XKFA/XKFD/XKFM/XKFS(EKF3 内部)+NKF0-5(EKF2)+VISO/VISP/VISV
+ORGN/GPS/IMU,首次可钻**。这是下一步。

## 3. P04-F.3 结论边界(严守)
- **AA003 未完成**;**A/A 无 telemetry 扰动结论**(ON 臂从未跑,OFF/ON 不可比较)。
- **OPEN-1 新证据**:①**首个"失败样本+BIN"受控复现**(LOG_DISARMED=1 交付失败臂 FCU 内部时序,消除 AA001
  no-BIN 盲区);②帧级坐实前窗墙钟慢启动(0.9Hz)+12 次>1000ms 打断,与 AA001-r3 同链强支持。
- **OPEN-1 仍未定位**:上游"为何慢启动"根因 UNKNOWN,须 BIN-EKF(XKF/VISO)内部时序分析。
- **禁**:不得写 telemetry 无扰动/WorldModel 已稳/OPEN-1 已关闭/WP307 过/长稳过/Review3 完成。
- **下一步**(单一申请):对 aa-r1 BIN 做 XKF/VISO/ORGN 内部时序分析(离线只读,写 dataflash 解码器,不改生产码),
  定位慢启动上游因;是否重跑 AA003(拿 ON 臂+多失败样本)由负责人裁决。

## 4. 边界
只读离线分析,未改生产码。LP 帧级用 scratchpad/open1_lp_replay/open1_lp32.py(纯 stdlib,复用 open1_tlog
帧同步;crc_failed=0)。BIN 消息类型清单用最小 FMT 扫描。深度 EKF 时序分析未做(下一步)。
